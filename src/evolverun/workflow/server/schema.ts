/**
 * Schema DDL for ClawWeb.
 *
 * Includes ClawFlow's tables (v1–v4) so ClawWeb can read them,
 * plus ClawWeb's own workflow_specs table (v5).
 * All DDL uses CREATE TABLE IF NOT EXISTS for safe co-existence.
 *
 * Uses SQLite-compatible SQL as the canonical dialect. Dialect-specific
 * rendering is handled by each Dialect implementation via `renderDdl`.
 *
 * Compliance (ZDAS/MySQL):
 * - id columns use BIGINT (MySQL) / INTEGER (SQLite)
 * - gmt_create / gmt_modified required on every table
 * - No FLOAT/DOUBLE columns (metric_value uses DECIMAL/INTEGER)
 * - MySQL: TIMESTAMP DEFAULT CURRENT_TIMESTAMP / ON UPDATE CURRENT_TIMESTAMP
 * - MySQL: All columns have COMMENT
 * - MySQL: Indexes defined inline within CREATE TABLE (no separate CREATE INDEX
 *          to avoid ZDAS parser class cast errors)
 * - MySQL: Indexed columns use VARCHAR(255) not TEXT (OceanBase can't index TEXT)
 * - SQLite: Uses AFTER UPDATE triggers for gmt_modified auto-update
 */

export type DbType = "sqlite" | "mysql" | "zdas";

/**
 * Trigger DDL for auto-updating gmt_modified on SQLite.
 * MySQL handles this via ON UPDATE CURRENT_TIMESTAMP, so these
 * are only needed for SQLite.
 */
export const sqliteTriggers: string[] = [
  `CREATE TRIGGER IF NOT EXISTS trg_flow_events_update AFTER UPDATE ON flow_events FOR EACH ROW BEGIN UPDATE flow_events SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_flow_metrics_update AFTER UPDATE ON flow_metrics FOR EACH ROW BEGIN UPDATE flow_metrics SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_triggered_alerts_update AFTER UPDATE ON triggered_alerts FOR EACH ROW BEGIN UPDATE triggered_alerts SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_node_executions_update AFTER UPDATE ON node_executions FOR EACH ROW BEGIN UPDATE node_executions SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_flow_runs_update AFTER UPDATE ON flow_runs FOR EACH ROW BEGIN UPDATE flow_runs SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_scheduled_triggers_update AFTER UPDATE ON scheduled_triggers FOR EACH ROW BEGIN UPDATE scheduled_triggers SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_webhook_triggers_update AFTER UPDATE ON webhook_triggers FOR EACH ROW BEGIN UPDATE webhook_triggers SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_webhook_events_update AFTER UPDATE ON webhook_events FOR EACH ROW BEGIN UPDATE webhook_events SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_workflow_specs_update AFTER UPDATE ON workflow_specs FOR EACH ROW BEGIN UPDATE workflow_specs SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_schema_version_update AFTER UPDATE ON schema_version FOR EACH ROW BEGIN UPDATE schema_version SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_knowledge_bases_update AFTER UPDATE ON knowledge_bases FOR EACH ROW BEGIN UPDATE knowledge_bases SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_validation_templates_update AFTER UPDATE ON validation_templates FOR EACH ROW BEGIN UPDATE validation_templates SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_facade_bindings_update AFTER UPDATE ON facade_bindings FOR EACH ROW BEGIN UPDATE facade_bindings SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_approval_cards_update AFTER UPDATE ON approval_cards FOR EACH ROW BEGIN UPDATE approval_cards SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_node_step_traces_update AFTER UPDATE ON node_step_traces FOR EACH ROW BEGIN UPDATE node_step_traces SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_bot_workflow_permissions_update AFTER UPDATE ON bot_workflow_permissions FOR EACH ROW BEGIN UPDATE bot_workflow_permissions SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_flow_control_slots_update AFTER UPDATE ON flow_control_slots FOR EACH ROW BEGIN UPDATE flow_control_slots SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_flow_control_queue_update AFTER UPDATE ON flow_control_queue FOR EACH ROW BEGIN UPDATE flow_control_queue SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_cm_bench_templates_update AFTER UPDATE ON cm_bench_templates FOR EACH ROW BEGIN UPDATE cm_bench_templates SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_cm_bench_runs_update AFTER UPDATE ON cm_bench_runs FOR EACH ROW BEGIN UPDATE cm_bench_runs SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_cm_bench_task_results_update AFTER UPDATE ON cm_bench_task_results FOR EACH ROW BEGIN UPDATE cm_bench_task_results SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_cm_bench_domains_update AFTER UPDATE ON cm_bench_domains FOR EACH ROW BEGIN UPDATE cm_bench_domains SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_cm_bench_template_versions_update AFTER UPDATE ON cm_bench_template_versions FOR EACH ROW BEGIN UPDATE cm_bench_template_versions SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_cm_bench_artifacts_update AFTER UPDATE ON cm_bench_artifacts FOR EACH ROW BEGIN UPDATE cm_bench_artifacts SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_workflow_notification_configs_update AFTER UPDATE ON workflow_notification_configs FOR EACH ROW BEGIN UPDATE workflow_notification_configs SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_dev_workflow_templates_update AFTER UPDATE ON dev_workflow_templates FOR EACH ROW BEGIN UPDATE dev_workflow_templates SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_dev_workflows_update AFTER UPDATE ON dev_workflows FOR EACH ROW BEGIN UPDATE dev_workflows SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_dev_workflow_phases_update AFTER UPDATE ON dev_workflow_phases FOR EACH ROW BEGIN UPDATE dev_workflow_phases SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_dima_work_items_update AFTER UPDATE ON dima_work_items FOR EACH ROW BEGIN UPDATE dima_work_items SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_conversation_logs_update AFTER UPDATE ON conversation_logs FOR EACH ROW BEGIN UPDATE conversation_logs SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_log_analysis_results_update AFTER UPDATE ON log_analysis_results FOR EACH ROW BEGIN UPDATE log_analysis_results SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_pending_operations_update AFTER UPDATE ON pending_operations FOR EACH ROW BEGIN UPDATE pending_operations SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_dev_git_ops_update AFTER UPDATE ON dev_git_ops FOR EACH ROW BEGIN UPDATE dev_git_ops SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_dev_artifacts_update AFTER UPDATE ON dev_artifacts FOR EACH ROW BEGIN UPDATE dev_artifacts SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  // dev_code_changes and dev_code_change_files triggers removed (tables dropped in v58)
  `CREATE TRIGGER IF NOT EXISTS trg_dev_phase_conversations_update AFTER UPDATE ON dev_phase_conversations FOR EACH ROW BEGIN UPDATE dev_phase_conversations SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_dev_approvals_update AFTER UPDATE ON dev_approvals FOR EACH ROW BEGIN UPDATE dev_approvals SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_dev_approval_reviewers_update AFTER UPDATE ON dev_approval_reviewers FOR EACH ROW BEGIN UPDATE dev_approval_reviewers SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  // dev_approval_decisions trigger removed (table dropped in v57)
  `CREATE TRIGGER IF NOT EXISTS trg_dev_discussions_update AFTER UPDATE ON dev_discussions FOR EACH ROW BEGIN UPDATE dev_discussions SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_dev_discussion_replies_update AFTER UPDATE ON dev_discussion_replies FOR EACH ROW BEGIN UPDATE dev_discussion_replies SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_dev_project_constraints_update AFTER UPDATE ON dev_project_constraints FOR EACH ROW BEGIN UPDATE dev_project_constraints SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_insight_failure_task_update AFTER UPDATE ON insight_failure_task FOR EACH ROW BEGIN UPDATE insight_failure_task SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_insight_metric_daily_update AFTER UPDATE ON insight_metric_daily FOR EACH ROW BEGIN UPDATE insight_metric_daily SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_insight_improvement_evidence_update AFTER UPDATE ON insight_improvement_evidence FOR EACH ROW BEGIN UPDATE insight_improvement_evidence SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_insight_improvement_evolve_link_update AFTER UPDATE ON insight_improvement_evolve_link FOR EACH ROW BEGIN UPDATE insight_improvement_evolve_link SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_ce_task_sources_update AFTER UPDATE ON ce_task_sources FOR EACH ROW BEGIN UPDATE ce_task_sources SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_insight_governance_rule_trust_update AFTER UPDATE ON insight_governance_rule_trust FOR EACH ROW BEGIN UPDATE insight_governance_rule_trust SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_insight_auto_repair_grant_update AFTER UPDATE ON insight_auto_repair_grant FOR EACH ROW BEGIN UPDATE insight_auto_repair_grant SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_ce_repair_tool_calls_update AFTER UPDATE ON ce_repair_tool_calls FOR EACH ROW BEGIN UPDATE ce_repair_tool_calls SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_flow_retry_requests_update AFTER UPDATE ON flow_retry_requests FOR EACH ROW BEGIN UPDATE flow_retry_requests SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
  `CREATE TRIGGER IF NOT EXISTS trg_workflow_deploy_history_update AFTER UPDATE ON workflow_deploy_history FOR EACH ROW BEGIN UPDATE workflow_deploy_history SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
];

/**
 * All migration definitions, in order.
 * Each migration uses SQLite-compatible SQL as the canonical form.
 * For MySQL/ZDAS: each Dialect's `renderDdl` converts types.
 * For SQLite: triggers for gmt_modified auto-update are in sqliteTriggers.
 *
 * Note: Indexed columns use VARCHAR(255) instead of TEXT because
 * OceanBase/MySQL cannot index TEXT columns. SQLite treats VARCHAR
 * identically to TEXT so this is fully compatible.
 */
export const migrations: ReadonlyArray<{ version: number; description: string; sql: string[]; sqliteOnly?: boolean; mysqlOnly?: boolean }> = [
  {
    version: 1,
    description: "ClawFlow: flow_events, flow_metrics, triggered_alerts",
    sql: [
      `CREATE TABLE IF NOT EXISTS flow_events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  event_id VARCHAR(255) NOT NULL,
  flow_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255),
  event_type VARCHAR(255) NOT NULL,
  attempt INTEGER,
  time INTEGER NOT NULL,
  data_json TEXT,
  error_text TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_events_flow_id ON flow_events (flow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_events_workflow_id ON flow_events (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_events_time ON flow_events (time)`,

      `CREATE TABLE IF NOT EXISTS flow_metrics (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  flow_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255) NOT NULL,
  metric_name VARCHAR(255) NOT NULL,
  metric_value DECIMAL(20,6) NOT NULL,
  time INTEGER NOT NULL,
  labels_json TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_metrics_workflow ON flow_metrics (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_metrics_name ON flow_metrics (metric_name)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_metrics_time ON flow_metrics (time)`,

      `CREATE TABLE IF NOT EXISTS triggered_alerts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  flow_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255),
  alert_rule VARCHAR(255) NOT NULL,
  severity VARCHAR(255) NOT NULL DEFAULT 'warning',
  message TEXT NOT NULL,
  time INTEGER NOT NULL,
  acknowledged INTEGER NOT NULL DEFAULT 0,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_triggered_alerts_workflow ON triggered_alerts (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_triggered_alerts_ack ON triggered_alerts (acknowledged)`,
      `CREATE INDEX IF NOT EXISTS idx_triggered_alerts_time ON triggered_alerts (time)`,
    ],
  },
  {
    version: 2,
    description: "ClawFlow: node_executions, flow_runs",
    sql: [
      `CREATE TABLE IF NOT EXISTS node_executions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  flow_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255) NOT NULL,
  executor_type VARCHAR(255),
  status VARCHAR(255) NOT NULL,
  attempt INTEGER NOT NULL DEFAULT 1,
  input_json TEXT,
  output_json TEXT,
  error_text TEXT,
  duration_ms BIGINT,
  token_usage_json TEXT,
  started_at BIGINT NOT NULL,
  completed_at BIGINT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_node_exec_flow_id ON node_executions (flow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_node_exec_workflow_id ON node_executions (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_node_exec_node_status ON node_executions (flow_id, node_id, status)`,
      `CREATE INDEX IF NOT EXISTS idx_node_exec_created ON node_executions (gmt_create)`,

      `CREATE TABLE IF NOT EXISTS flow_runs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  flow_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  workflow_title VARCHAR(255),
  status VARCHAR(255) NOT NULL,
  params_json TEXT,
  input_json TEXT,
  result_json TEXT,
  node_count INTEGER NOT NULL DEFAULT 0,
  succeeded_count INTEGER NOT NULL DEFAULT 0,
  failed_count INTEGER NOT NULL DEFAULT 0,
  total_duration_ms BIGINT,
  total_token_usage BIGINT,
  started_at BIGINT NOT NULL,
  completed_at BIGINT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_flow_runs_flow_id ON flow_runs (flow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_runs_workflow_id ON flow_runs (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_runs_status ON flow_runs (status)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_runs_started ON flow_runs (started_at)`,
    ],
  },
  {
    version: 3,
    description: "ClawFlow: scheduled_triggers",
    sql: [
      `CREATE TABLE IF NOT EXISTS scheduled_triggers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  trigger_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  pack_id VARCHAR(255) NOT NULL,
  cron_expression VARCHAR(255) NOT NULL,
  timezone VARCHAR(255) NOT NULL DEFAULT 'UTC',
  params_json TEXT,
  max_concurrent INTEGER NOT NULL DEFAULT 1,
  enabled INTEGER NOT NULL DEFAULT 1,
  last_fire_time INTEGER,
  next_fire_time INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_sched_triggers_workflow ON scheduled_triggers (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_sched_triggers_enabled_next ON scheduled_triggers (enabled, next_fire_time)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_sched_triggers_trigger_id ON scheduled_triggers (trigger_id)`,
    ],
  },
  {
    version: 4,
    description: "ClawFlow: webhook_triggers, webhook_events",
    sql: [
      `CREATE TABLE IF NOT EXISTS webhook_triggers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  trigger_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  pack_id VARCHAR(255),
  secret VARCHAR(255),
  payload_mapping TEXT,
  allowed_ips TEXT,
  enabled INTEGER NOT NULL DEFAULT 1,
  description TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_webhook_triggers_workflow ON webhook_triggers (workflow_id)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_webhook_triggers_trigger_id ON webhook_triggers (trigger_id)`,

      `CREATE TABLE IF NOT EXISTS webhook_events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  event_id VARCHAR(255) NOT NULL,
  trigger_id VARCHAR(255) NOT NULL,
  flow_id VARCHAR(255),
  status VARCHAR(255) NOT NULL,
  request_method VARCHAR(255) NOT NULL,
  request_headers TEXT,
  request_body_hash VARCHAR(255),
  response_code INTEGER,
  error_message TEXT,
  ip_address VARCHAR(255),
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_webhook_events_trigger ON webhook_events (trigger_id)`,
      `CREATE INDEX IF NOT EXISTS idx_webhook_events_event_id ON webhook_events (event_id)`,
      `CREATE INDEX IF NOT EXISTS idx_webhook_events_created ON webhook_events (gmt_create)`,
      `CREATE INDEX IF NOT EXISTS idx_webhook_events_dedup ON webhook_events (event_id, gmt_create)`,
    ],
  },
  {
    version: 5,
    description: "ClawWeb: workflow_specs for browser-persisted edits",
    sql: [
      `CREATE TABLE IF NOT EXISTS workflow_specs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  pack_id VARCHAR(255),
  spec_json MEDIUMTEXT NOT NULL,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_workflow_specs_workflow_id ON workflow_specs (workflow_id)`,
    ],
  },
  {
    version: 6,
    description: "ClawWeb: add triggered_by to flow_runs, node_title to node_executions",
    sql: [
      `ALTER TABLE flow_runs ADD COLUMN triggered_by VARCHAR(255)`,
      `ALTER TABLE node_executions ADD COLUMN node_title VARCHAR(255)`,
      `ALTER TABLE node_executions ADD COLUMN triggered_by VARCHAR(255)`,
    ],
  },
  {
    version: 8,
    description: "ClawWeb: knowledge_bases table for GRT knowledge base configuration",
    sql: [
      `CREATE TABLE IF NOT EXISTS knowledge_bases (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  kb_id VARCHAR(255) NOT NULL,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  instance_name VARCHAR(255) NOT NULL,
  interface_name VARCHAR(255) NOT NULL,
  token VARCHAR(255) NOT NULL,
  user_name VARCHAR(255) NOT NULL DEFAULT '',
  user_id VARCHAR(255) NOT NULL DEFAULT '',
  top_k INTEGER NOT NULL DEFAULT 3,
  ranking_threshold REAL NOT NULL DEFAULT 0.01,
  vector_threshold REAL NOT NULL DEFAULT 0.6,
  ranking_model VARCHAR(255) NOT NULL DEFAULT 'bge-reranker-base',
  env VARCHAR(255) NOT NULL DEFAULT 'prod',
  enabled INTEGER NOT NULL DEFAULT 1,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_knowledge_bases_kb_id ON knowledge_bases (kb_id)`,
      `CREATE INDEX IF NOT EXISTS idx_knowledge_bases_enabled ON knowledge_bases (enabled)`,
      `CREATE INDEX IF NOT EXISTS idx_knowledge_bases_created ON knowledge_bases (gmt_create)`,
      // Seed a default GRT knowledge base entry
      `INSERT OR IGNORE INTO knowledge_bases (kb_id, name, description, instance_name, interface_name, token, user_name, user_id, top_k, ranking_threshold, vector_threshold, ranking_model, env, enabled) VALUES ('my-grt-kb', 'Default GRT Knowledge Base', 'Default GRT knowledge base for ClawFlow workflows', 'TeamClawBot', 'ALL_TeamClawBot', '', '', '', 3, 0.01, 0.6, 'bge-reranker-base', 'prod', 0)`,
    ],
  },
  {
    version: 7,
    description: "ClawWeb: fix schema_version and workflow_specs column names for trigger compatibility",
    sqliteOnly: true,
    sql: [
      // schema_version was created without id/gmt_create/gmt_modified — rebuild it
      // SQLite can't ALTER to add PK, so we recreate with the expected schema
      `CREATE TABLE schema_version_v7 (id INTEGER PRIMARY KEY AUTOINCREMENT, version INTEGER NOT NULL, description TEXT, gmt_create INTEGER NOT NULL DEFAULT (unixepoch()), gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()))`,
      `INSERT INTO schema_version_v7 (id, version, description, gmt_create, gmt_modified) SELECT rowid, version, description, applied_at, applied_at FROM schema_version`,
      `DROP TABLE schema_version`,
      `ALTER TABLE schema_version_v7 RENAME TO schema_version`,
      // workflow_specs used created_at/updated_at instead of gmt_create/gmt_modified
      `ALTER TABLE workflow_specs RENAME COLUMN created_at TO gmt_create`,
      `ALTER TABLE workflow_specs RENAME COLUMN updated_at TO gmt_modified`,
      // Recreate the trigger to match the renamed column
      `DROP TRIGGER IF EXISTS trg_workflow_specs_update`,
      `CREATE TRIGGER trg_workflow_specs_update AFTER UPDATE ON workflow_specs FOR EACH ROW BEGIN UPDATE workflow_specs SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
    ],
  },
  {
    version: 9,
    description: "ClawWeb: validation_templates table for task validation",
    sql: [
      `CREATE TABLE IF NOT EXISTS validation_templates (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  template_id VARCHAR(255) NOT NULL,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  content TEXT NOT NULL,
  enabled INTEGER NOT NULL DEFAULT 1,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_validation_templates_template_id ON validation_templates (template_id)`,
      `CREATE INDEX IF NOT EXISTS idx_validation_templates_enabled ON validation_templates (enabled)`,
    ],
  },
  {
    version: 10,
    description: "ClawFlow: add branch_id to node_executions for conditional branch routing",
    sql: [
      `ALTER TABLE node_executions ADD COLUMN branch_id VARCHAR(255)`,
    ],
  },
  {
    version: 11,
    description: "ClawWeb: facade_bindings table for command-to-workflow facade binding",
    sql: [
      `CREATE TABLE IF NOT EXISTS facade_bindings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  command VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  pack_id VARCHAR(255),
  remark TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_facade_bindings_command ON facade_bindings (command)`,
      `CREATE INDEX IF NOT EXISTS idx_facade_bindings_workflow ON facade_bindings (workflow_id)`,
    ],
  },
  {
    version: 12,
    description: "ClawWeb: add identity_key/current_phase to flow_runs, progress_message to node_executions, event_type/payload_json/received_at to webhook_events",
    sql: [
      `ALTER TABLE flow_runs ADD COLUMN identity_key TEXT`,
      `ALTER TABLE flow_runs ADD COLUMN current_phase VARCHAR(255)`,
      `ALTER TABLE node_executions ADD COLUMN progress_message TEXT`,
      `ALTER TABLE webhook_events ADD COLUMN event_type VARCHAR(255)`,
      `ALTER TABLE webhook_events ADD COLUMN payload_json TEXT`,
      `ALTER TABLE webhook_events ADD COLUMN received_at INTEGER`,
    ],
  },
  {
    version: 13,
    description: "ClawWeb: rename created_at/updated_at to gmt_create/gmt_modified for consistency",
    sqliteOnly: true,
    sql: [
      // flow_runs
      `ALTER TABLE flow_runs RENAME COLUMN created_at TO gmt_create`,
      `ALTER TABLE flow_runs RENAME COLUMN updated_at TO gmt_modified`,
      // node_executions
      `ALTER TABLE node_executions RENAME COLUMN created_at TO gmt_create`,
      // scheduled_triggers
      `ALTER TABLE scheduled_triggers RENAME COLUMN created_at TO gmt_create`,
      `ALTER TABLE scheduled_triggers RENAME COLUMN updated_at TO gmt_modified`,
      // webhook_triggers
      `ALTER TABLE webhook_triggers RENAME COLUMN created_at TO gmt_create`,
      `ALTER TABLE webhook_triggers RENAME COLUMN updated_at TO gmt_modified`,
      // webhook_events
      `ALTER TABLE webhook_events RENAME COLUMN created_at TO gmt_create`,
      // flow_metrics
      `ALTER TABLE flow_metrics RENAME COLUMN created_at TO gmt_create`,
      // triggered_alerts
      `ALTER TABLE triggered_alerts RENAME COLUMN created_at TO gmt_create`,
      // Add missing gmt_modified columns where they don't exist
      `ALTER TABLE node_executions ADD COLUMN gmt_modified INTEGER`,
      `ALTER TABLE flow_metrics ADD COLUMN gmt_modified INTEGER`,
      `ALTER TABLE triggered_alerts ADD COLUMN gmt_modified INTEGER`,
      `ALTER TABLE webhook_events ADD COLUMN gmt_modified INTEGER`,
      // Fix the update triggers to reference gmt_modified
      `DROP TRIGGER IF EXISTS trg_flow_runs_update`,
      `CREATE TRIGGER trg_flow_runs_update AFTER UPDATE ON flow_runs FOR EACH ROW BEGIN UPDATE flow_runs SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
      `DROP TRIGGER IF EXISTS trg_scheduled_triggers_update`,
      `CREATE TRIGGER trg_scheduled_triggers_update AFTER UPDATE ON scheduled_triggers FOR EACH ROW BEGIN UPDATE scheduled_triggers SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
      `DROP TRIGGER IF EXISTS trg_webhook_triggers_update`,
      `CREATE TRIGGER trg_webhook_triggers_update AFTER UPDATE ON webhook_triggers FOR EACH ROW BEGIN UPDATE webhook_triggers SET gmt_modified = (unixepoch()) WHERE id = NEW.id; END`,
    ],
  },
  {
    version: 14,
    description: "Add session_key and session_id to node_executions",
    sql: [
      `ALTER TABLE node_executions ADD COLUMN session_key VARCHAR(255)`,
      `ALTER TABLE node_executions ADD COLUMN session_id VARCHAR(255)`,
    ],
  },
  {
    version: 15,
    description: "Approval cards table for card-web delivery",
    sql: [
      `CREATE TABLE IF NOT EXISTS approval_cards (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  flow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  workflow_title VARCHAR(255),
  approval_type VARCHAR(255),
  message TEXT,
  card_fields_json TEXT,
  approver_ids TEXT NOT NULL,
  approver_names TEXT,
  approval_policy VARCHAR(50) NOT NULL DEFAULT 'any',
  approved_by VARCHAR(4000) NOT NULL DEFAULT '',
  rejected_by VARCHAR(4000) NOT NULL DEFAULT '',
  status VARCHAR(50) NOT NULL DEFAULT 'pending',
  delivery_mode VARCHAR(50) NOT NULL DEFAULT 'card-web',
  created_at INTEGER NOT NULL,
  resolved_at INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_approval_cards_flow_node ON approval_cards (flow_id, node_id)`,
      `CREATE INDEX IF NOT EXISTS idx_approval_cards_status ON approval_cards (status)`,
      `CREATE INDEX IF NOT EXISTS idx_approval_cards_created ON approval_cards (created_at)`,
    ],
  },
  {
    version: 16,
    description: "Add system_context_json to node_executions for recording trigger evaluation, hook outcomes, retry context, executor details, and human actions",
    sql: [
      `ALTER TABLE node_executions ADD COLUMN system_context_json TEXT`,
    ],
  },
  {
    version: 17,
    description: "ClawFlow: flow_control_slots and flow_control_queue tables for concurrency throttling",
    sql: [
      `CREATE TABLE IF NOT EXISTS flow_control_slots (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  instance_id VARCHAR(255) NOT NULL,
  scope_key VARCHAR(255) NOT NULL,
  flow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255),
  acquired_at INTEGER NOT NULL,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_fc_slots_instance_scope_flow_node ON flow_control_slots (instance_id, scope_key, flow_id, node_id)`,
      `CREATE INDEX IF NOT EXISTS idx_fc_slots_instance_scope ON flow_control_slots (instance_id, scope_key)`,

      `CREATE TABLE IF NOT EXISTS flow_control_queue (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  instance_id VARCHAR(255) NOT NULL,
  scope_key VARCHAR(255) NOT NULL,
  flow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255),
  priority INTEGER NOT NULL DEFAULT 0,
  status VARCHAR(32) NOT NULL DEFAULT 'queued',
  enqueued_at INTEGER NOT NULL,
  dispatch_after INTEGER,
  expires_at INTEGER,
  payload TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_fc_queue_instance_scope_status ON flow_control_queue (instance_id, scope_key, status, priority, enqueued_at)`,
      `CREATE INDEX IF NOT EXISTS idx_fc_queue_expires ON flow_control_queue (expires_at, status)`,
      `CREATE INDEX IF NOT EXISTS idx_fc_queue_instance_flow ON flow_control_queue (instance_id, flow_id)`,
    ],
  },
  {
    version: 18,
    description: "ClawWeb: bot_workflow_permissions table for bot-to-workflow access control",
    sql: [
      `CREATE TABLE IF NOT EXISTS bot_workflow_permissions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  bot_id VARCHAR(255),
  bot_owner_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  env VARCHAR(255) NOT NULL DEFAULT 'dev',
  can_view INTEGER NOT NULL DEFAULT 0,
  can_execute INTEGER NOT NULL DEFAULT 0,
  can_edit INTEGER NOT NULL DEFAULT 0,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_bot_wf_perm ON bot_workflow_permissions (bot_id, bot_owner_id, workflow_id, env)`,
      `CREATE INDEX IF NOT EXISTS idx_bot_wf_perm_workflow ON bot_workflow_permissions (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_bot_wf_perm_owner ON bot_workflow_permissions (bot_owner_id, workflow_id, env)`,
    ],
  },
  {
    version: 19,
    description: "Normalize empty string bot_id to NULL for owner-level permissions",
    sql: [
      `UPDATE bot_workflow_permissions SET bot_id = NULL WHERE bot_id = ''`,
    ],
  },
  {
    version: 20,
    description: "ClawWeb: add structured columns to validation_templates for baseline metadata",
    sql: [
      `ALTER TABLE validation_templates ADD COLUMN category VARCHAR(64)`,
      `ALTER TABLE validation_templates ADD COLUMN grading_type VARCHAR(64)`,
      `ALTER TABLE validation_templates ADD COLUMN timeout_seconds INTEGER`,
      `ALTER TABLE validation_templates ADD COLUMN grading_weights_json TEXT`,
      `CREATE INDEX IF NOT EXISTS idx_validation_templates_category ON validation_templates (category)`,
      `CREATE INDEX IF NOT EXISTS idx_validation_templates_grading_type ON validation_templates (grading_type)`,
    ],
  },
  {
    version: 21,
    description: "ClawFlow: add session_id to flow_control_slots for session-liveness-based zombie detection",
    sql: [
      `ALTER TABLE flow_control_slots ADD COLUMN session_id VARCHAR(255)`,
      `CREATE INDEX IF NOT EXISTS idx_fc_slots_session_id ON flow_control_slots (session_id)`,
    ],
  },
  {
    version: 22,
    description: "ClawFlow: add lease-based flow control columns (lease_expires_at, renew_count) to replace zombie detection with heartbeat",
    sql: [
      `ALTER TABLE flow_control_slots ADD COLUMN lease_expires_at INTEGER NOT NULL DEFAULT 0`,
      `ALTER TABLE flow_control_slots ADD COLUMN renew_count INTEGER NOT NULL DEFAULT 0`,
      `CREATE INDEX IF NOT EXISTS idx_fc_slots_lease_expiry ON flow_control_slots (instance_id, lease_expires_at)`,
    ],
  },
  {
    version: 23,
    description: "ClawFlow: human intervention - credentials and session tracking columns on flow_runs",
    sql: [
      `ALTER TABLE flow_runs ADD COLUMN credentials_json TEXT`,
      `ALTER TABLE flow_runs ADD COLUMN origin_session_key VARCHAR(512)`,
      `ALTER TABLE flow_runs ADD COLUMN origin_session_id VARCHAR(255)`,
      `ALTER TABLE flow_runs ADD COLUMN origin_bot_id VARCHAR(255)`,
    ],
  },
  {
    version: 24,
    description: "Bench domain-aware redesign: drop old tables, create cm_bench_domains, cm_bench_templates, cm_bench_template_versions, cm_bench_runs, cm_bench_task_results",
    sql: [
      `DROP TABLE IF EXISTS cm_bench_templates`,
      `DROP TABLE IF EXISTS cm_bench_runs`,
      `DROP TABLE IF EXISTS cm_bench_task_results`,
      `DROP INDEX IF EXISTS uk_cm_bench_templates_template_version`,
      `DROP INDEX IF EXISTS idx_cm_bench_templates_task`,
      `DROP INDEX IF EXISTS idx_cm_bench_templates_status`,
      `DROP INDEX IF EXISTS idx_cm_bench_templates_target`,
      `DROP INDEX IF EXISTS uk_cm_bench_runs_run_id`,
      `DROP INDEX IF EXISTS idx_cm_bench_runs_template`,
      `DROP INDEX IF EXISTS idx_cm_bench_runs_task`,
      `DROP INDEX IF EXISTS idx_cm_bench_runs_status`,
      `DROP INDEX IF EXISTS idx_cm_bench_runs_clawmind_flow`,
      `DROP INDEX IF EXISTS uk_cm_bench_task_results_result_id`,
      `DROP INDEX IF EXISTS idx_cm_bench_task_results_run`,
      `DROP INDEX IF EXISTS idx_cm_bench_task_results_task`,

      `CREATE TABLE IF NOT EXISTS cm_bench_domains (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  domain_id VARCHAR(128) NOT NULL,
  name VARCHAR(255) NOT NULL,
  description TEXT DEFAULT NULL,
  source_root VARCHAR(1024) DEFAULT NULL,
  created_by VARCHAR(128) DEFAULT NULL,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_cm_bench_domains_domain_id ON cm_bench_domains (domain_id)`,

      `CREATE TABLE IF NOT EXISTS cm_bench_templates (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  domain_id VARCHAR(128) NOT NULL,
  template_name VARCHAR(255) NOT NULL,
  display_name VARCHAR(255) DEFAULT NULL,
  description TEXT DEFAULT NULL,
  category VARCHAR(128) DEFAULT NULL,
  target_type VARCHAR(64) NOT NULL DEFAULT 'agent_session',
  grading_type VARCHAR(64) NOT NULL DEFAULT 'automated',
  source VARCHAR(64) NOT NULL DEFAULT 'agentbench',
  source_path VARCHAR(1024) DEFAULT NULL,
  source_hash VARCHAR(128) DEFAULT NULL,
  latest_version INT NOT NULL DEFAULT 1,
  published_version INT DEFAULT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'draft',
  created_by VARCHAR(128) DEFAULT NULL,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_cm_bench_templates_domain_name ON cm_bench_templates (domain_id, template_name)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_templates_domain_status ON cm_bench_templates (domain_id, status)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_templates_domain_name ON cm_bench_templates (domain_id, template_name)`,

      `CREATE TABLE IF NOT EXISTS cm_bench_template_versions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  domain_id VARCHAR(128) NOT NULL,
  template_name VARCHAR(255) NOT NULL,
  version INT NOT NULL,
  display_name VARCHAR(255) DEFAULT NULL,
  description TEXT DEFAULT NULL,
  content_md MEDIUMTEXT NOT NULL,
  parsed_meta_json MEDIUMTEXT DEFAULT NULL,
  source_path VARCHAR(1024) DEFAULT NULL,
  source_hash VARCHAR(128) DEFAULT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'draft',
  created_by VARCHAR(128) DEFAULT NULL,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_cm_bench_template_versions_domain_name_version ON cm_bench_template_versions (domain_id, template_name, version)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_template_versions_domain_name_status ON cm_bench_template_versions (domain_id, template_name, status)`,

      `CREATE TABLE IF NOT EXISTS cm_bench_runs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  bench_run_id VARCHAR(128) NOT NULL,
  domain_id VARCHAR(128) NOT NULL,
  template_name VARCHAR(255) NOT NULL,
  template_version INT NOT NULL,
  target_type VARCHAR(64) NOT NULL DEFAULT 'agent_session',
  status VARCHAR(32) NOT NULL DEFAULT 'pending',
  score REAL DEFAULT NULL,
  max_score REAL DEFAULT NULL,
  pass_rate REAL DEFAULT NULL,
  model VARCHAR(255) DEFAULT NULL,
  suite VARCHAR(255) DEFAULT NULL,
  scene VARCHAR(255) DEFAULT NULL,
  triggered_by VARCHAR(128) DEFAULT NULL,
  clawmind_flow_id VARCHAR(128) DEFAULT NULL,
  session_id VARCHAR(128) DEFAULT NULL,
  session_key VARCHAR(128) DEFAULT NULL,
  run_config_json MEDIUMTEXT DEFAULT NULL,
  summary_json MEDIUMTEXT DEFAULT NULL,
  error_text MEDIUMTEXT DEFAULT NULL,
  started_at BIGINT DEFAULT NULL,
  completed_at BIGINT DEFAULT NULL,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_cm_bench_runs_run_id ON cm_bench_runs (bench_run_id)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_runs_domain_template ON cm_bench_runs (domain_id, template_name)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_runs_status ON cm_bench_runs (status)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_runs_clawmind_flow ON cm_bench_runs (clawmind_flow_id)`,

      `CREATE TABLE IF NOT EXISTS cm_bench_task_results (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  result_id VARCHAR(128) NOT NULL,
  bench_run_id VARCHAR(128) NOT NULL,
  task_id VARCHAR(128) NOT NULL,
  task_name VARCHAR(255) DEFAULT NULL,
  status VARCHAR(32) NOT NULL,
  score REAL DEFAULT NULL,
  max_score REAL DEFAULT NULL,
  grading_type VARCHAR(64) DEFAULT NULL,
  execution_time_ms BIGINT DEFAULT NULL,
  transcript_path VARCHAR(1024) DEFAULT NULL,
  workspace_path VARCHAR(1024) DEFAULT NULL,
  result_json MEDIUMTEXT DEFAULT NULL,
  breakdown_json MEDIUMTEXT DEFAULT NULL,
  notes MEDIUMTEXT DEFAULT NULL,
  error_text MEDIUMTEXT DEFAULT NULL,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_cm_bench_task_results_result_id ON cm_bench_task_results (result_id)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_task_results_run ON cm_bench_task_results (bench_run_id)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_task_results_task ON cm_bench_task_results (task_id)`,
    ],
  },
  {
    version: 25,
    description: "Bench user isolation: add owner_user_id to domain/template/run tables and switch to composite unique keys",
    sql: [
      `ALTER TABLE cm_bench_domains ADD COLUMN owner_user_id VARCHAR(128) NOT NULL DEFAULT ''`,
      `UPDATE cm_bench_domains SET owner_user_id = COALESCE(created_by, '') WHERE owner_user_id = ''`,
      `DROP INDEX IF EXISTS uk_cm_bench_domains_domain_id`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_cm_bench_domains_owner_domain ON cm_bench_domains (owner_user_id, domain_id)`,

      `ALTER TABLE cm_bench_templates ADD COLUMN owner_user_id VARCHAR(128) NOT NULL DEFAULT ''`,
      `UPDATE cm_bench_templates SET owner_user_id = COALESCE(created_by, '') WHERE owner_user_id = ''`,
      `DROP INDEX IF EXISTS uk_cm_bench_templates_domain_name`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_cm_bench_templates_owner_domain_name ON cm_bench_templates (owner_user_id, domain_id, template_name)`,

      `ALTER TABLE cm_bench_template_versions ADD COLUMN owner_user_id VARCHAR(128) NOT NULL DEFAULT ''`,
      `UPDATE cm_bench_template_versions SET owner_user_id = COALESCE(created_by, '') WHERE owner_user_id = ''`,
      `DROP INDEX IF EXISTS uk_cm_bench_template_versions_domain_name_version`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_cm_btv_owner_dom_name_ver ON cm_bench_template_versions (owner_user_id, domain_id, template_name, version)`,

      `ALTER TABLE cm_bench_runs ADD COLUMN owner_user_id VARCHAR(128) NOT NULL DEFAULT ''`,
    ],
  },
  {
    version: 26,
    description: "Bench domain archive status",
    sql: [
      `ALTER TABLE cm_bench_domains ADD COLUMN status VARCHAR(32) NOT NULL DEFAULT 'active'`,
    ],
  },
  {
    version: 27,
    description: "Bench artifacts table for centralized report and session storage",
    sql: [
      `CREATE TABLE IF NOT EXISTS cm_bench_artifacts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  artifact_id VARCHAR(128) NOT NULL,
  bench_run_id VARCHAR(128) NOT NULL,
  result_id VARCHAR(128) DEFAULT NULL,
  task_id VARCHAR(255) DEFAULT NULL,
  artifact_type VARCHAR(64) NOT NULL,
  filename VARCHAR(512) DEFAULT NULL,
  content_type VARCHAR(128) DEFAULT NULL,
  size_bytes BIGINT DEFAULT NULL,
  storage_type VARCHAR(32) NOT NULL DEFAULT 'db',
  storage_path VARCHAR(2048) DEFAULT NULL,
  content_text MEDIUMTEXT DEFAULT NULL,
  content_json MEDIUMTEXT DEFAULT NULL,
  summary_json MEDIUMTEXT DEFAULT NULL,
  sha256 VARCHAR(128) DEFAULT NULL,
  created_by VARCHAR(128) DEFAULT NULL,
  owner_user_id VARCHAR(128) NOT NULL DEFAULT '',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_cm_bench_artifacts_artifact_id ON cm_bench_artifacts (artifact_id)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_artifacts_run ON cm_bench_artifacts (bench_run_id)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_artifacts_run_type ON cm_bench_artifacts (bench_run_id, artifact_type)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_artifacts_task ON cm_bench_artifacts (bench_run_id, task_id)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_bench_artifacts_owner_run ON cm_bench_artifacts (owner_user_id, bench_run_id)`,
    ],
  },
  {
    version: 28,
    description: "Add node_step_traces table for recording embedded-agent execution steps",
    sql: [
      `CREATE TABLE IF NOT EXISTS node_step_traces (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  flow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255) NOT NULL,
  attempt INTEGER NOT NULL DEFAULT 1,
  step_seq INTEGER NOT NULL,
  step_type VARCHAR(32) NOT NULL,
  skill_name VARCHAR(255),
  tool_name VARCHAR(255),
  tool_use_id VARCHAR(255),
  tool_input_json TEXT,
  tool_output_text TEXT,
  is_error INTEGER NOT NULL DEFAULT 0,
  text_content TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_nst_flow_node ON node_step_traces (flow_id, node_id, attempt)`,
      `CREATE INDEX IF NOT EXISTS idx_nst_flow_id ON node_step_traces (flow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_nst_skill_name ON node_step_traces (skill_name)`,
      `CREATE INDEX IF NOT EXISTS idx_nst_created ON node_step_traces (gmt_create)`,
      `CREATE TRIGGER IF NOT EXISTS trg_node_step_traces_update
AFTER UPDATE ON node_step_traces FOR EACH ROW
BEGIN
  UPDATE node_step_traces SET gmt_modified = (unixepoch()) WHERE id = NEW.id;
END`,
    ],
  },
  {
    version: 29,
    description: "Add node_hallucination_checks table for rule-based hallucination detection",
    sql: [
      `CREATE TABLE IF NOT EXISTS node_hallucination_checks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  flow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255) NOT NULL,
  attempt INTEGER NOT NULL DEFAULT 1,
  check_type VARCHAR(32) NOT NULL,
  severity VARCHAR(16) NOT NULL DEFAULT 'low',
  passed INTEGER NOT NULL DEFAULT 1,
  description TEXT,
  evidence TEXT,
  risk_score INTEGER NOT NULL DEFAULT 0,
  risk_level VARCHAR(16) NOT NULL DEFAULT 'none',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_nhc_flow_node ON node_hallucination_checks (flow_id, node_id, attempt)`,
      `CREATE INDEX IF NOT EXISTS idx_nhc_flow_id ON node_hallucination_checks (flow_id)`,
      `CREATE TRIGGER IF NOT EXISTS trg_node_hallucination_checks_update
AFTER UPDATE ON node_hallucination_checks FOR EACH ROW
BEGIN
  UPDATE node_hallucination_checks SET gmt_modified = (unixepoch()) WHERE id = NEW.id;
END`,
    ],
  },
  {
    version: 30,
    description: "Add embedded_session_key to node_executions for Langfuse trace correlation",
    sql: [
      `ALTER TABLE node_executions ADD COLUMN embedded_session_key VARCHAR(512)`,
    ],
  },
  {
    version: 31,
    description: "Add Langfuse correlation and observability fields to node_step_traces",
    sql: [
      `ALTER TABLE node_step_traces ADD COLUMN session_key VARCHAR(512)`,
      `ALTER TABLE node_step_traces ADD COLUMN trace_id VARCHAR(64)`,
      `ALTER TABLE node_step_traces ADD COLUMN observation_id VARCHAR(64)`,
      `ALTER TABLE node_step_traces ADD COLUMN model VARCHAR(255)`,
      `ALTER TABLE node_step_traces ADD COLUMN latency_ms BIGINT`,
      `ALTER TABLE node_step_traces ADD COLUMN prompt_tokens BIGINT`,
      `ALTER TABLE node_step_traces ADD COLUMN completion_tokens BIGINT`,
    ],
  },
  {
    version: 32,
    description: "Add title column to workflow_specs to avoid loading spec_json in list views",
    sql: [
      `ALTER TABLE workflow_specs ADD COLUMN title VARCHAR(255)`,
    ],
  },
  {
    version: 33,
    description: "Add workflow_notification_configs table for per-workflow DingTalk failure notification settings",
    sql: [
      `CREATE TABLE IF NOT EXISTS workflow_notification_configs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  robot_code VARCHAR(255) NOT NULL,
  app_secret VARCHAR(255) NOT NULL,
  on_failure_users TEXT NOT NULL,
  on_failure_groups TEXT NOT NULL,
  on_failure_message_title VARCHAR(255),
  on_failure_message_include_run_link INTEGER NOT NULL DEFAULT 1,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_notification_configs_workflow ON workflow_notification_configs (workflow_id)`,
    ],
  },
  {
    version: 34,
    description: "Add user_id to flow_runs for tracking who triggered the workflow",
    sql: [
      `ALTER TABLE flow_runs ADD COLUMN user_id VARCHAR(255)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_runs_user_id ON flow_runs (user_id)`,
    ],
  },
  {
    version: 36,
    description: "Add plugin_version to flow_runs for engine version tracking",
    sql: [
      `ALTER TABLE flow_runs ADD COLUMN plugin_version VARCHAR(255) DEFAULT NULL`,
    ],
  },
  {
    version: 35,
    description: "Auto-dev: dev_workflow_templates table for configurable workflow phase definitions",
    sql: [
      `CREATE TABLE IF NOT EXISTS dev_workflow_templates (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  template_id VARCHAR(255) NOT NULL,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  phases_json MEDIUMTEXT NOT NULL,
  is_built_in INTEGER NOT NULL DEFAULT 0,
  created_by VARCHAR(128) NOT NULL DEFAULT '',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dev_wf_templates_template_id ON dev_workflow_templates (template_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_wf_templates_built_in ON dev_workflow_templates (is_built_in)`,
    ],
  },
  {
    version: 36,
    description: "Auto-dev: dev_workflows table for workflow instance state machine",
    sql: [
      `CREATE TABLE IF NOT EXISTS dev_workflows (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  title VARCHAR(255) NOT NULL,
  template_id VARCHAR(255) NOT NULL,
  dima_work_item_id VARCHAR(255) NOT NULL,
  dima_work_item_type VARCHAR(32) NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'pending',
  current_phase VARCHAR(64),
  enabled_phases_json TEXT NOT NULL,
  config_json TEXT,
  git_repo_url VARCHAR(1024),
  git_branch VARCHAR(512),
  pr_url VARCHAR(1024),
  pr_id VARCHAR(255),
  timeout_hours INTEGER NOT NULL DEFAULT 72,
  owner_user_id VARCHAR(128) NOT NULL DEFAULT '',
  started_at INTEGER,
  completed_at INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dev_workflows_workflow_id ON dev_workflows (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_workflows_status ON dev_workflows (status)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_workflows_dima_id ON dev_workflows (dima_work_item_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_workflows_template ON dev_workflows (template_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_workflows_owner ON dev_workflows (owner_user_id)`,
    ],
  },
  {
    version: 37,
    description: "Auto-dev: dev_workflow_phases table for per-phase execution tracking with optimistic locking",
    sql: [
      `CREATE TABLE IF NOT EXISTS dev_workflow_phases (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  phase_id VARCHAR(64) NOT NULL,
  phase_name VARCHAR(255) NOT NULL,
  phase_order INTEGER NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'pending',
  enabled INTEGER NOT NULL DEFAULT 1,
  required INTEGER NOT NULL DEFAULT 1,
  has_human_gate INTEGER NOT NULL DEFAULT 0,
  has_bot_execution INTEGER NOT NULL DEFAULT 1,
  bot_role VARCHAR(128),
  default_timeout_minutes INTEGER NOT NULL DEFAULT 10,
  document_url VARCHAR(2048),
  document_title VARCHAR(512),
  result_summary MEDIUMTEXT,
  baas_run_id VARCHAR(255),
  error_message TEXT,
  confirmed_by VARCHAR(128),
  confirm_comment TEXT,
  rejected_by VARCHAR(128),
  reject_reason TEXT,
  reject_to_phase_id VARCHAR(64),
  version INTEGER NOT NULL DEFAULT 0,
  started_at INTEGER,
  completed_at INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dev_wf_phases_wf_phase ON dev_workflow_phases (workflow_id, phase_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_wf_phases_workflow ON dev_workflow_phases (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_wf_phases_status ON dev_workflow_phases (workflow_id, status)`,
    ],
  },
  {
    version: 38,
    description: "Auto-dev: dima_work_items table for Dima work item local cache with sync tracking",
    sql: [
      `CREATE TABLE IF NOT EXISTS dima_work_items (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  dima_id VARCHAR(255) NOT NULL,
  dima_type VARCHAR(32) NOT NULL,
  subject VARCHAR(512) NOT NULL,
  content MEDIUMTEXT,
  status VARCHAR(64),
  priority VARCHAR(32),
  processor VARCHAR(255),
  creator VARCHAR(255),
  project_id VARCHAR(64),
  workspace_id VARCHAR(64),
  tenant_id VARCHAR(64),
  linked_workflow_id VARCHAR(255),
  dima_gmt_create INTEGER,
  dima_gmt_modified INTEGER,
  synced_at INTEGER NOT NULL,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dima_work_items_dima_id ON dima_work_items (dima_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dima_work_items_type ON dima_work_items (dima_type)`,
      `CREATE INDEX IF NOT EXISTS idx_dima_work_items_status ON dima_work_items (status)`,
      `CREATE INDEX IF NOT EXISTS idx_dima_work_items_synced ON dima_work_items (synced_at)`,
      `CREATE INDEX IF NOT EXISTS idx_dima_work_items_linked ON dima_work_items (linked_workflow_id)`,
    ],
  },
  {
    version: 39,
    description: "Auto-dev: log_analysis_results table for LLM-driven log error analysis with cooldown dedup",
    sql: [
      `CREATE TABLE IF NOT EXISTS log_analysis_results (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  analysis_id VARCHAR(255) NOT NULL,
  error_pattern VARCHAR(1024) NOT NULL,
  error_count INTEGER NOT NULL DEFAULT 1,
  root_cause TEXT,
  severity VARCHAR(16) NOT NULL DEFAULT 'medium',
  fix_suggestion TEXT,
  estimated_changed_files INTEGER,
  is_known_issue INTEGER NOT NULL DEFAULT 0,
  related_bug_id VARCHAR(255),
  dima_bug_id VARCHAR(255),
  linked_workflow_id VARCHAR(255),
  status VARCHAR(32) NOT NULL DEFAULT 'new',
  log_source VARCHAR(255),
  lookback_minutes INTEGER NOT NULL DEFAULT 30,
  analysis_json MEDIUMTEXT,
  cooldown_until INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_log_analysis_analysis_id ON log_analysis_results (analysis_id)`,
      `CREATE INDEX IF NOT EXISTS idx_log_analysis_status ON log_analysis_results (status)`,
      `CREATE INDEX IF NOT EXISTS idx_log_analysis_severity ON log_analysis_results (severity)`,
      `CREATE INDEX IF NOT EXISTS idx_log_analysis_pattern ON log_analysis_results (error_pattern)`,
      `CREATE INDEX IF NOT EXISTS idx_log_analysis_cooldown ON log_analysis_results (cooldown_until)`,
    ],
  },
  {
    version: 40,
    description: "Auto-dev: pending_operations table for retry-queue on Dima/YuQue write failures",
    sql: [
      `CREATE TABLE IF NOT EXISTS pending_operations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  op_id VARCHAR(255) NOT NULL,
  op_type VARCHAR(64) NOT NULL,
  target_id VARCHAR(255) NOT NULL,
  payload_json MEDIUMTEXT NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'pending',
  attempt_count INTEGER NOT NULL DEFAULT 0,
  max_attempts INTEGER NOT NULL DEFAULT 3,
  next_retry_at INTEGER,
  last_error TEXT,
  workflow_id VARCHAR(255),
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_pending_ops_op_id ON pending_operations (op_id)`,
      `CREATE INDEX IF NOT EXISTS idx_pending_ops_status ON pending_operations (status, next_retry_at)`,
      `CREATE INDEX IF NOT EXISTS idx_pending_ops_workflow ON pending_operations (workflow_id)`,
    ],
  },
  {
    version: 41,
    description: "ClawFix: clawfix_known_issues table for error-hash deduplication",
    sql: [
      `CREATE TABLE IF NOT EXISTS clawfix_known_issues (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  error_hash VARCHAR(64) NOT NULL,
  module VARCHAR(64) NOT NULL,
  first_seen INTEGER NOT NULL DEFAULT (unixepoch()),
  last_seen INTEGER NOT NULL DEFAULT (unixepoch()),
  occurrence_count INTEGER NOT NULL DEFAULT 1,
  dima_bug_id VARCHAR(255),
  status VARCHAR(16) NOT NULL DEFAULT 'active',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_known_issues_hash ON clawfix_known_issues (error_hash)`,
      `CREATE INDEX IF NOT EXISTS idx_known_issues_status ON clawfix_known_issues (status)`,
    ],
  },
  {
    version: 42,
    description: "ClawFix: clawfix_run_history table for pipeline run tracking",
    sql: [
      `CREATE TABLE IF NOT EXISTS clawfix_run_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  run_id VARCHAR(64) NOT NULL,
  started_at INTEGER NOT NULL DEFAULT (unixepoch()),
  completed_at INTEGER,
  issues_found INTEGER NOT NULL DEFAULT 0,
  dima_created INTEGER NOT NULL DEFAULT 0,
  prs_created INTEGER NOT NULL DEFAULT 0,
  skipped INTEGER NOT NULL DEFAULT 0,
  errors_json TEXT,
  status VARCHAR(16) NOT NULL DEFAULT 'running',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_run_history_id ON clawfix_run_history (run_id)`,
      `CREATE INDEX IF NOT EXISTS idx_run_history_status ON clawfix_run_history (status)`,
    ],
  },
  {
    version: 43,
    description: "ClawFix: clawfix_ocb_fixes table for OCB PR/fix record tracking",
    sql: [
      `CREATE TABLE IF NOT EXISTS clawfix_ocb_fixes (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  fix_id VARCHAR(64) NOT NULL,
  analysis_id VARCHAR(255) NOT NULL,
  dima_bug_id VARCHAR(255),
  module VARCHAR(64) NOT NULL,
  branch_name VARCHAR(255),
  pr_url VARCHAR(512),
  commit_sha VARCHAR(64),
  status VARCHAR(16) NOT NULL DEFAULT 'pending',
  error_message TEXT,
  strategy VARCHAR(16) NOT NULL DEFAULT 'mcp',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_ocb_fixes_id ON clawfix_ocb_fixes (fix_id)`,
      `CREATE INDEX IF NOT EXISTS idx_ocb_fixes_analysis ON clawfix_ocb_fixes (analysis_id)`,
    ],
  },
  {
    version: 44,
    description: "Conversation logging: conversation_logs table for user↔AI dialog persistence across channels",
    sql: [
      `CREATE TABLE IF NOT EXISTS conversation_logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  bot_id VARCHAR(255) NOT NULL,
  session_key VARCHAR(255) NOT NULL,
  session_id VARCHAR(255),
  sender_id VARCHAR(255) NOT NULL,
  channel VARCHAR(64) NOT NULL DEFAULT 'unknown',
  sender_name VARCHAR(255),
  group_name VARCHAR(255),
  input_data TEXT,
  output_data MEDIUMTEXT,
  ref_url TEXT,
  status VARCHAR(32) NOT NULL DEFAULT 'pending',
  response_rank VARCHAR(255),
  response_rank_tag VARCHAR(255),
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_conversation_logs_bot_session ON conversation_logs (bot_id, session_key)`,
      `CREATE INDEX IF NOT EXISTS idx_conversation_logs_session_id ON conversation_logs (session_id)`,
      `CREATE INDEX IF NOT EXISTS idx_conversation_logs_sender ON conversation_logs (sender_id)`,
      `CREATE INDEX IF NOT EXISTS idx_conversation_logs_channel ON conversation_logs (channel)`,
      `CREATE INDEX IF NOT EXISTS idx_conversation_logs_status ON conversation_logs (status)`,
      `CREATE INDEX IF NOT EXISTS idx_conversation_logs_created ON conversation_logs (gmt_create)`,
    ],
  },
  {
    version: 45,
    description: "ClawFix: add steps_json column to clawfix_run_history for per-step progress tracking",
    sql: [
      `ALTER TABLE clawfix_run_history ADD COLUMN steps_json TEXT`,
    ],
  },
  {
    version: 46,
    description: "ClawFix: add issues_pending_action column to clawfix_run_history for dry-run tracking",
    sql: [
      `ALTER TABLE clawfix_run_history ADD COLUMN issues_pending_action INTEGER NOT NULL DEFAULT 0`,
    ],
  },
  {
    version: 47,
    description: "ClawFix: add error_hash to log_analysis_results for known-issue dedup writeback",
    sql: [
      `ALTER TABLE log_analysis_results ADD COLUMN error_hash VARCHAR(64)`,
      `CREATE INDEX IF NOT EXISTS idx_log_analysis_error_hash ON log_analysis_results (error_hash)`,
    ],
  },
  {
    version: 48,
    description: "ClawFix: drop clawfix_ocb_fixes and remove ocb_fix_id from known_issues — PR/fix pipeline removed",
    sql: [
      `DROP TABLE IF EXISTS clawfix_ocb_fixes`,
      `ALTER TABLE clawfix_known_issues DROP COLUMN ocb_fix_id`,
    ],
  },
  {
    version: 49,
    description: "Dev-workflow enhancement: dev_git_ops table for tracking BOT git operations",
    sql: [
      `CREATE TABLE IF NOT EXISTS dev_git_ops (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  phase_id VARCHAR(64) NOT NULL,
  operation VARCHAR(32) NOT NULL,
  repo_url VARCHAR(1024) NOT NULL,
  branch VARCHAR(512) NOT NULL,
  commit_sha VARCHAR(64),
  commit_message VARCHAR(1024),
  remote_branch VARCHAR(512),
  result VARCHAR(16) NOT NULL DEFAULT 'success',
  error_message TEXT,
  executed_by VARCHAR(128) NOT NULL DEFAULT '',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  CONSTRAINT chk_git_ops_operation CHECK (operation IN ('clone', 'pull', 'checkout', 'commit', 'push')),
  CONSTRAINT chk_git_ops_result CHECK (result IN ('success', 'failed', 'timeout'))
)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_git_ops_workflow ON dev_git_ops (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_git_ops_wf_phase ON dev_git_ops (workflow_id, phase_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_git_ops_commit ON dev_git_ops (commit_sha)`,
    ],
  },
  {
    version: 50,
    description: "Dev-workflow enhancement: dev_artifacts table for phase artifact versioning",
    sql: [
      `CREATE TABLE IF NOT EXISTS dev_artifacts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  phase_id VARCHAR(64) NOT NULL,
  artifact_type VARCHAR(64) NOT NULL,
  version INTEGER NOT NULL DEFAULT 1,
  title VARCHAR(512) NOT NULL,
  content MEDIUMTEXT,
  content_url VARCHAR(2048),
  format VARCHAR(32) NOT NULL DEFAULT 'markdown',
  status VARCHAR(32) NOT NULL DEFAULT 'current',
  source VARCHAR(32) NOT NULL DEFAULT 'bot',
  authored_by VARCHAR(128) NOT NULL DEFAULT '',
  archived_at INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  CONSTRAINT chk_artifact_status CHECK (status IN ('draft', 'current', 'archived')),
  CONSTRAINT chk_artifact_source CHECK (source IN ('bot', 'human', 'imported')),
  CONSTRAINT chk_artifact_format CHECK (format IN ('markdown', 'yaml', 'json', 'html'))
)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_artifacts_workflow ON dev_artifacts (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_artifacts_wf_phase ON dev_artifacts (workflow_id, phase_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_artifacts_type_status ON dev_artifacts (workflow_id, artifact_type, status)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dev_artifacts_wf_phase_type_ver ON dev_artifacts (workflow_id, phase_id, artifact_type, version)`,
    ],
  },
  {
    version: 51,
    description: "Dev-workflow enhancement: dev_code_changes + dev_code_change_files tables",
    sql: [
      `CREATE TABLE IF NOT EXISTS dev_code_changes (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  phase_id VARCHAR(64) NOT NULL,
  summary VARCHAR(1024) NOT NULL,
  change_scope VARCHAR(32) NOT NULL DEFAULT 'phase',
  total_files INTEGER NOT NULL DEFAULT 0,
  additions INTEGER NOT NULL DEFAULT 0,
  deletions INTEGER NOT NULL DEFAULT 0,
  commit_sha VARCHAR(64),
  branch VARCHAR(512),
  status VARCHAR(32) NOT NULL DEFAULT 'reported',
  reported_by VARCHAR(128) NOT NULL DEFAULT '',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  CONSTRAINT chk_change_status CHECK (status IN ('reported', 'reviewed', 'approved', 'rejected', 'superseded'))
)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_code_changes_workflow ON dev_code_changes (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_code_changes_wf_phase ON dev_code_changes (workflow_id, phase_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_code_changes_commit ON dev_code_changes (commit_sha)`,
      `CREATE TABLE IF NOT EXISTS dev_code_change_files (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  change_id INTEGER NOT NULL,
  file_path VARCHAR(1024) NOT NULL,
  change_type VARCHAR(16) NOT NULL DEFAULT 'modified',
  diff_summary VARCHAR(255),
  additions INTEGER NOT NULL DEFAULT 0,
  deletions INTEGER NOT NULL DEFAULT 0,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  CONSTRAINT chk_file_change_type CHECK (change_type IN ('added', 'modified', 'deleted', 'renamed')),
  FOREIGN KEY (change_id) REFERENCES dev_code_changes(id)
)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_change_files_change ON dev_code_change_files (change_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_change_files_path ON dev_code_change_files (file_path)`,
    ],
  },
  {
    version: 52,
    description: "Dev-workflow enhancement: dev_phase_conversations table for conversation persistence",
    sql: [
      `CREATE TABLE IF NOT EXISTS dev_phase_conversations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  phase_id VARCHAR(64) NOT NULL,
  baas_message_id VARCHAR(255),
  role VARCHAR(16) NOT NULL,
  sender_id VARCHAR(128) NOT NULL DEFAULT '',
  sender_name VARCHAR(255),
  content MEDIUMTEXT NOT NULL,
  session_id VARCHAR(255),
  bot_id VARCHAR(255),
  metadata_json TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  CONSTRAINT chk_conv_role CHECK (role IN ('user', 'assistant', 'system'))
)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_phase_conv_workflow ON dev_phase_conversations (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_phase_conv_wf_phase ON dev_phase_conversations (workflow_id, phase_id)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dev_phase_conv_baas_msg ON dev_phase_conversations (baas_message_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_phase_conv_session ON dev_phase_conversations (session_id)`,
    ],
  },
  {
    version: 53,
    description: "Dev-workflow enhancement: dev_approvals + dev_approval_reviewers + dev_approval_decisions tables",
    sql: [
      `CREATE TABLE IF NOT EXISTS dev_approvals (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  approval_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  phase_id VARCHAR(64) NOT NULL,
  target_type VARCHAR(32) NOT NULL,
  target_id VARCHAR(255) NOT NULL,
  title VARCHAR(512) NOT NULL,
  description TEXT,
  status VARCHAR(32) NOT NULL DEFAULT 'pending',
  block_phase INTEGER NOT NULL DEFAULT 0,
  version INTEGER NOT NULL DEFAULT 0,
  created_by VARCHAR(128) NOT NULL DEFAULT '',
  resolved_at INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  CONSTRAINT chk_approval_status CHECK (status IN ('pending', 'approved', 'rejected', 'cancelled', 'expired')),
  CONSTRAINT chk_approval_target CHECK (target_type IN ('artifact', 'code_change', 'phase'))
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dev_approvals_approval_id ON dev_approvals (approval_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_approvals_workflow ON dev_approvals (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_approvals_wf_phase ON dev_approvals (workflow_id, phase_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_approvals_target ON dev_approvals (target_type, target_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_approvals_status ON dev_approvals (status)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_approvals_created_by ON dev_approvals (created_by)`,
      `CREATE TABLE IF NOT EXISTS dev_approval_reviewers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  approval_id VARCHAR(255) NOT NULL,
  reviewer_id VARCHAR(128) NOT NULL,
  reviewer_name VARCHAR(255),
  notified INTEGER NOT NULL DEFAULT 0,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  FOREIGN KEY (approval_id) REFERENCES dev_approvals(approval_id)
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dev_approval_rev_approval_user ON dev_approval_reviewers (approval_id, reviewer_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_approval_rev_reviewer ON dev_approval_reviewers (reviewer_id)`,
      `CREATE TABLE IF NOT EXISTS dev_approval_decisions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  approval_id VARCHAR(255) NOT NULL,
  reviewer_id VARCHAR(128) NOT NULL,
  reviewer_name VARCHAR(255),
  decision VARCHAR(16) NOT NULL,
  comment TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  CONSTRAINT chk_decision CHECK (decision IN ('approved', 'rejected', 'commented')),
  FOREIGN KEY (approval_id) REFERENCES dev_approvals(approval_id)
)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_approval_dec_approval ON dev_approval_decisions (approval_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_approval_dec_reviewer ON dev_approval_decisions (reviewer_id)`,
    ],
  },
  {
    version: 54,
    description: "Dev-workflow enhancement: dev_discussions + dev_discussion_participants + dev_discussion_replies tables",
    sql: [
      `CREATE TABLE IF NOT EXISTS dev_discussions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  discussion_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  phase_id VARCHAR(64) NOT NULL,
  topic VARCHAR(512) NOT NULL,
  description TEXT,
  context_type VARCHAR(32),
  context_id VARCHAR(255),
  status VARCHAR(32) NOT NULL DEFAULT 'open',
  conclusion TEXT,
  created_by VARCHAR(128) NOT NULL DEFAULT '',
  closed_by VARCHAR(128),
  closed_at INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  CONSTRAINT chk_discussion_status CHECK (status IN ('open', 'closed')),
  CONSTRAINT chk_discussion_context CHECK (context_type IS NULL OR context_type IN ('artifact', 'code_change', 'phase'))
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dev_discussions_disc_id ON dev_discussions (discussion_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_discussions_workflow ON dev_discussions (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_discussions_wf_phase ON dev_discussions (workflow_id, phase_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_discussions_status ON dev_discussions (status)`,
      `CREATE TABLE IF NOT EXISTS dev_discussion_participants (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  discussion_id VARCHAR(255) NOT NULL,
  user_id VARCHAR(128) NOT NULL,
  user_name VARCHAR(255),
  role VARCHAR(16) NOT NULL DEFAULT 'participant',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  CONSTRAINT chk_disc_part_role CHECK (role IN ('owner', 'participant')),
  FOREIGN KEY (discussion_id) REFERENCES dev_discussions(discussion_id)
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dev_disc_part_disc_user ON dev_discussion_participants (discussion_id, user_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_disc_part_user ON dev_discussion_participants (user_id)`,
      `CREATE TABLE IF NOT EXISTS dev_discussion_replies (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  discussion_id VARCHAR(255) NOT NULL,
  author_id VARCHAR(128) NOT NULL,
  author_name VARCHAR(255),
  content MEDIUMTEXT NOT NULL,
  parent_reply_id INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  FOREIGN KEY (discussion_id) REFERENCES dev_discussions(discussion_id),
  FOREIGN KEY (parent_reply_id) REFERENCES dev_discussion_replies(id)
)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_disc_rep_disc ON dev_discussion_replies (discussion_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_disc_rep_author ON dev_discussion_replies (author_id)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_disc_rep_parent ON dev_discussion_replies (parent_reply_id)`,
    ],
  },
  {
    version: 55,
    description: "Dev-workflow enhancement: dev_project_constraints table for project constraint versioning",
    sql: [
      `CREATE TABLE IF NOT EXISTS dev_project_constraints (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  version INTEGER NOT NULL DEFAULT 1,
  constraints_json MEDIUMTEXT NOT NULL,
  change_summary VARCHAR(512),
  changed_by VARCHAR(128) NOT NULL DEFAULT '',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_dev_proj_constraints_workflow ON dev_project_constraints (workflow_id)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_dev_proj_constraints_wf_ver ON dev_project_constraints (workflow_id, version)`,
    ],
  },
  {
    version: 56,
    description: "SQL performance: add composite index on flow_runs(status, started_at) to eliminate filesort; add index on flow_control_queue(status) to eliminate full table scan",
    sql: [
      `CREATE INDEX IF NOT EXISTS idx_flow_runs_status_started ON flow_runs (status, started_at)`,
      `CREATE INDEX IF NOT EXISTS idx_fc_queue_status ON flow_control_queue (status)`,
    ],
  },
  {
    version: 57,
    description: "Refactor: merge dev_approval_decisions into dev_approval_reviewers (add decision columns + migrate data); add summary to dev_git_ops",
    sql: [
      `ALTER TABLE dev_approval_reviewers ADD COLUMN decision VARCHAR(16) DEFAULT NULL`,
      `ALTER TABLE dev_approval_reviewers ADD COLUMN comment TEXT DEFAULT NULL`,
      `ALTER TABLE dev_approval_reviewers ADD COLUMN decided_at INTEGER DEFAULT NULL`,
      `ALTER TABLE dev_git_ops ADD COLUMN summary TEXT DEFAULT NULL`,
      `UPDATE dev_approval_reviewers SET decision = (SELECT d.decision FROM dev_approval_decisions d WHERE d.approval_id = dev_approval_reviewers.approval_id AND d.reviewer_id = dev_approval_reviewers.reviewer_id), comment = (SELECT d.comment FROM dev_approval_decisions d WHERE d.approval_id = dev_approval_reviewers.approval_id AND d.reviewer_id = dev_approval_reviewers.reviewer_id), decided_at = (SELECT d.gmt_create FROM dev_approval_decisions d WHERE d.approval_id = dev_approval_reviewers.approval_id AND d.reviewer_id = dev_approval_reviewers.reviewer_id) WHERE EXISTS (SELECT 1 FROM dev_approval_decisions d WHERE d.approval_id = dev_approval_reviewers.approval_id AND d.reviewer_id = dev_approval_reviewers.reviewer_id)`,
    ],
  },
  {
    version: 58,
    description: "Refactor: drop dev_code_changes and dev_code_change_files tables (replaced by Git API queries); drop dev_approval_decisions table (merged into reviewers)",
    sql: [
      `DROP TABLE IF EXISTS dev_code_change_files`,
      `DROP TABLE IF EXISTS dev_code_changes`,
      `DROP TABLE IF EXISTS dev_approval_decisions`,
    ],
  },
  {
    version: 59,
    description: "Refactor: drop dev_discussion_participants (participants derived from created_by + replies)",
    sql: [
      `DROP TABLE IF EXISTS dev_discussion_participants`,
    ],
  },
  {
    version: 60,
    description: "Add execution_step_log table for dynamic workflow execution observability",
    sql: [
      `CREATE TABLE IF NOT EXISTS execution_step_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  flow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255) NOT NULL,
  step_type VARCHAR(32) NOT NULL,
  timestamp BIGINT NOT NULL,
  input_summary TEXT,
  output_summary TEXT,
  llm_evaluation TEXT,
  decision_path TEXT,
  duration_ms BIGINT,
  token_usage BIGINT,
  metadata TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_esl_flow_id ON execution_step_log (flow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_esl_flow_node ON execution_step_log (flow_id, node_id)`,
      `CREATE INDEX IF NOT EXISTS idx_esl_step_type ON execution_step_log (flow_id, step_type)`,
    ],
  },
  {
    version: 61,
    description: "Fix INT overflow: change overflow-prone INTEGER columns to BIGINT for MySQL/OceanBase compatibility (MySQL INT max 2,147,483,647; flow_runs.total_duration_ms can exceed this for long-running zombie flows)",
    sql: [
      // flow_runs: total_duration_ms is the primary overflow victim
      // (ranForSecs * 1000 can exceed INT max when flow runs > ~24.8 days)
      `ALTER TABLE flow_runs MODIFY COLUMN total_duration_ms BIGINT`,
      `ALTER TABLE flow_runs MODIFY COLUMN total_token_usage BIGINT`,
      `ALTER TABLE flow_runs MODIFY COLUMN started_at BIGINT`,
      `ALTER TABLE flow_runs MODIFY COLUMN completed_at BIGINT`,
      // node_executions: duration_ms and timestamps same risk
      `ALTER TABLE node_executions MODIFY COLUMN duration_ms BIGINT`,
      `ALTER TABLE node_executions MODIFY COLUMN started_at BIGINT`,
      `ALTER TABLE node_executions MODIFY COLUMN completed_at BIGINT`,
      // node_step_traces: latency and token counters
      `ALTER TABLE node_step_traces MODIFY COLUMN latency_ms BIGINT`,
      `ALTER TABLE node_step_traces MODIFY COLUMN prompt_tokens BIGINT`,
      `ALTER TABLE node_step_traces MODIFY COLUMN completion_tokens BIGINT`,
      // execution_step_log: duration and token counters
      `ALTER TABLE execution_step_log MODIFY COLUMN duration_ms BIGINT`,
      `ALTER TABLE execution_step_log MODIFY COLUMN token_usage BIGINT`,
      `ALTER TABLE execution_step_log MODIFY COLUMN timestamp BIGINT`,
    ],
  },
  {
    version: 62,
    description: "Dev-workflow phases: add bot_id, prompt_template, approvers_json, approval_policy, prompt_resolved columns for per-phase Bot dispatch and approval control",
    sql: [
      `ALTER TABLE dev_workflow_phases ADD COLUMN bot_id VARCHAR(128)`,
      `ALTER TABLE dev_workflow_phases ADD COLUMN prompt_template TEXT`,
      `ALTER TABLE dev_workflow_phases ADD COLUMN prompt_variables_json TEXT`,
      `ALTER TABLE dev_workflow_phases ADD COLUMN approvers_json TEXT`,
      `ALTER TABLE dev_workflow_phases ADD COLUMN approval_policy VARCHAR(32) DEFAULT 'any'`,
      `ALTER TABLE dev_workflow_phases ADD COLUMN prompt_resolved TEXT`,
      `ALTER TABLE dev_workflow_phases ADD COLUMN gate_position VARCHAR(16) DEFAULT 'post-bot'`,
      `ALTER TABLE dev_workflow_phases ADD COLUMN confirmed_by_json TEXT`,
    ],
  },
  {
    version: 63,
    description: "Add http_callback_configs table for HTTP callback notification system",
    sql: [
      `CREATE TABLE IF NOT EXISTS http_callback_configs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  config_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  name VARCHAR(255) NOT NULL,
  url VARCHAR(1024) NOT NULL,
  secret VARCHAR(1024),
  enabled INTEGER NOT NULL DEFAULT 1,
  notify_on TEXT NOT NULL,
  timeout_ms INTEGER NOT NULL DEFAULT 5000,
  max_retries INTEGER NOT NULL DEFAULT 2,
  retry_delay_ms INTEGER NOT NULL DEFAULT 1000,
  include_node_output INTEGER NOT NULL DEFAULT 0,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (config_id)
)`,
      `CREATE INDEX IF NOT EXISTS idx_http_callback_configs_workflow ON http_callback_configs (workflow_id)`,
    ],
  },
  {
    version: 64,
    description: "Add workflow_deploy_history table for centralized deploy tracking and version column to workflow_specs",
    mysqlOnly: true,
    sql: [
      `CREATE TABLE IF NOT EXISTS workflow_deploy_history (
  \`id\` bigint NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  \`pack_id\` varchar(255) NOT NULL COMMENT '包ID，与git tag前缀一致',
  \`workflow_id\` varchar(255) NOT NULL COMMENT 'workflow唯一标识',
  \`deploy_number\` int NOT NULL COMMENT '全局包维度的序列号(对应#N)',
  \`version\` int NOT NULL COMMENT '该workflow在该deploy中的版本号，由部署系统统一管理，不与YAML耦合',
  \`tag_name\` varchar(255) NOT NULL COMMENT '关联的git tag名称，如deploy/my-pack/#3，审计锚点',
  \`action\` varchar(32) NOT NULL COMMENT 'deploy/rollback/pull/migration',
  \`from_deploy_number\` int DEFAULT NULL COMMENT '回滚时来源deploy #(action=rollback时填写)',
  \`spec_json\` mediumtext NOT NULL COMMENT '该次部署时workflow spec的完整JSON快照，回滚时的真相源',
  \`note\` varchar(512) DEFAULT NULL COMMENT '可选备注',
  \`bot_id\` varchar(255) DEFAULT NULL COMMENT '触发部署的bot ID',
  \`owner_id\` varchar(255) DEFAULT NULL COMMENT '部署发起人的staff_id，用于审计和权限追溯',
  \`gmt_create\` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  \`gmt_modified\` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  PRIMARY KEY (\`id\`),
  UNIQUE KEY \`uk_wdh_pack_deploy_wf\` (\`pack_id\`, \`deploy_number\`, \`workflow_id\`),
  KEY \`idx_wdh_workflow_version\` (\`workflow_id\`, \`version\`),
  KEY \`idx_wdh_workflow_deploy\` (\`workflow_id\`, \`deploy_number\` DESC),
  KEY \`idx_wdh_tag\` (\`tag_name\`),
  KEY \`idx_wdh_pack_deploy\` (\`pack_id\`, \`deploy_number\` DESC)
) DEFAULT CHARSET = utf8mb4`,
      `ALTER TABLE \`workflow_specs\` ADD COLUMN \`version\` int DEFAULT NULL COMMENT 'workflow当前已部署的版本号，NULL表示从未部署过'`,
      `ALTER TABLE \`workflow_specs\` ADD INDEX \`idx_wfs_workflow_version\` (\`workflow_id\`, \`version\`)`,
    ],
  },
  {
    version: 65,
    description: "Allow null tag_name in deploy_history for edit actions",
    mysqlOnly: true,
    sql: [
      `ALTER TABLE \`workflow_deploy_history\` MODIFY COLUMN \`tag_name\` varchar(255) DEFAULT NULL COMMENT '关联的git tag名称，edit操作无tag'`,
    ],
  },
  {
    version: 66,
    description: "Make http_callback_configs.secret nullable — MySQL",
    mysqlOnly: true,
    sql: [
      `ALTER TABLE http_callback_configs MODIFY COLUMN secret VARCHAR(1024) NULL COMMENT 'HMAC-SHA256 signing secret (optional)'`,
    ],
  },
  {
    version: 67,
    description: "Make http_callback_configs.secret nullable — SQLite",
    sqliteOnly: true,
    sql: [
      `ALTER TABLE http_callback_configs RENAME TO http_callback_configs_old`,
      `CREATE TABLE http_callback_configs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  config_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  name VARCHAR(255) NOT NULL,
  url VARCHAR(1024) NOT NULL,
  secret VARCHAR(1024),
  enabled INTEGER NOT NULL DEFAULT 1,
  notify_on TEXT NOT NULL,
  timeout_ms INTEGER NOT NULL DEFAULT 5000,
  max_retries INTEGER NOT NULL DEFAULT 2,
  retry_delay_ms INTEGER NOT NULL DEFAULT 1000,
  include_node_output INTEGER NOT NULL DEFAULT 0,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (config_id)
)`,
      `INSERT INTO http_callback_configs SELECT * FROM http_callback_configs_old`,
      `DROP TABLE http_callback_configs_old`,
      `CREATE INDEX IF NOT EXISTS idx_http_callback_configs_workflow ON http_callback_configs (workflow_id)`,
    ],
  },
  {
    version: 68,
    description: "Change deploy_history UK to include version — allows multiple edit records with deploy_number=0",
    mysqlOnly: true,
    sql: [
      `ALTER TABLE workflow_deploy_history DROP INDEX uk_wdh_pack_deploy_wf`,
      `ALTER TABLE workflow_deploy_history ADD UNIQUE KEY uk_wdh_pack_deploy_wf (pack_id, deploy_number, workflow_id, version)`,
    ],
  },
  {
    version: 69,
    description: "Add http_callback_logs table for HTTP callback audit logging",
    sql: [
      `CREATE TABLE IF NOT EXISTS http_callback_logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  flow_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  config_id VARCHAR(255) NOT NULL,
  config_name VARCHAR(255),
  callback_url VARCHAR(1024) NOT NULL,
  notify_event VARCHAR(64) NOT NULL,
  node_id VARCHAR(255),
  attempt INTEGER NOT NULL DEFAULT 0,
  max_attempts INTEGER NOT NULL DEFAULT 1,
  request_body TEXT,
  request_headers TEXT,
  response_status_code INTEGER,
  response_body TEXT,
  duration_ms INTEGER,
  status VARCHAR(32) NOT NULL DEFAULT 'sent',
  error_message TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_http_callback_logs_flow ON http_callback_logs (flow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_http_callback_logs_workflow ON http_callback_logs (workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_http_callback_logs_status ON http_callback_logs (status)`,
      `CREATE INDEX IF NOT EXISTS idx_http_callback_logs_config ON http_callback_logs (config_id)`,
    ],
  },
  {
    version: 70,
    description: "Add engine column to flow_runs for tracking which host platform started the flow",
    sql: [
      `ALTER TABLE flow_runs ADD COLUMN engine VARCHAR(255) DEFAULT NULL`,
    ],
  },
  {
    version: 71,
    description: "Add cm_app_config table for DB-stored ClawMind application configuration",
    sql: [
      `CREATE TABLE IF NOT EXISTS cm_app_config (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  config_key VARCHAR(64) NOT NULL,
  config_yaml TEXT NOT NULL,
  version INTEGER NOT NULL DEFAULT 1,
  enabled INTEGER NOT NULL DEFAULT 1,
  description TEXT,
  updated_by VARCHAR(255),
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_cm_app_config_key ON cm_app_config (config_key)`,
      `CREATE INDEX IF NOT EXISTS idx_cm_app_config_enabled ON cm_app_config (enabled)`,
    ],
  },
  {
    version: 72,
    description: "Add final Claw evolution MVP task and step tables",
    sql: [
      `CREATE TABLE IF NOT EXISTS ce_tasks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  task_id VARCHAR(64) NOT NULL,
  task_name VARCHAR(128) NOT NULL,
  remark VARCHAR(1000),
  task_type VARCHAR(32) NOT NULL,
  user_id VARCHAR(128) NOT NULL,
  bot_id VARCHAR(128) NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'pending',
  config_json TEXT NOT NULL,
  error_message TEXT,
  created_by VARCHAR(128) NOT NULL,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (task_id)
)`,
      `CREATE INDEX IF NOT EXISTS idx_ce_tasks_object ON ce_tasks (user_id, bot_id, gmt_create)`,
      `CREATE INDEX IF NOT EXISTS idx_ce_tasks_status ON ce_tasks (status, gmt_create)`,
      `CREATE TABLE IF NOT EXISTS ce_steps (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  step_id VARCHAR(64) NOT NULL,
  task_id VARCHAR(64) NOT NULL,
  step_type VARCHAR(32) NOT NULL,
  step_no INTEGER NOT NULL,
  round_no INTEGER,
  command VARCHAR(1000) NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'created',
  bot_run_id VARCHAR(255),
  bot_session_id VARCHAR(255),
  bot_response_json TEXT,
  output_json TEXT,
  summary TEXT,
  error_code VARCHAR(128),
  error_message TEXT,
  retryable INTEGER,
  started_at INTEGER,
  completed_at INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (step_id),
  UNIQUE (task_id, step_no)
)`,
      `CREATE INDEX IF NOT EXISTS idx_ce_steps_order ON ce_steps (task_id, step_no)`,
      `CREATE INDEX IF NOT EXISTS idx_ce_steps_status ON ce_steps (task_id, status)`,
      `CREATE INDEX IF NOT EXISTS idx_ce_steps_bot_run ON ce_steps (bot_run_id)`,
    ],
  },
  {
    version: 73,
    description: "Create run_logs table for console log capture (run archive feature)",
    sql: [
      `CREATE TABLE IF NOT EXISTS run_logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  flow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255),
  level VARCHAR(32) NOT NULL,
  source VARCHAR(255),
  message TEXT NOT NULL,
  timestamp BIGINT NOT NULL,
  seq INTEGER NOT NULL,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE INDEX IF NOT EXISTS idx_run_logs_flow_id ON run_logs (flow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_run_logs_flow_node ON run_logs (flow_id, node_id)`,
      `CREATE INDEX IF NOT EXISTS idx_run_logs_level ON run_logs (flow_id, level)`,
    ],
  },
  {
    version: 74,
    description: "Add resolved_prompt column to node_executions (ClawMind-owned; idempotent — column may already exist on shared DBs)",
    sql: [
      `ALTER TABLE node_executions ADD COLUMN resolved_prompt TEXT`,
    ],
  },
  {
    version: 75,
    description: "Add Insight Center improvement item, evidence snapshot, and Evolve link tables",
    sql: [
      `CREATE TABLE IF NOT EXISTS insight_improvement_item (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  owner_user_id VARCHAR(128) NOT NULL DEFAULT '',
  bot_id VARCHAR(128) NOT NULL DEFAULT '',
  title VARCHAR(256) NOT NULL DEFAULT '',
  user_guidance TEXT,
  source_type VARCHAR(32) NOT NULL DEFAULT 'USER_SELECTED',
  source_rule_id VARCHAR(64),
  evidence_count INTEGER NOT NULL DEFAULT 0,
  session_count INTEGER NOT NULL DEFAULT 0,
  data_start_time VARCHAR(32),
  data_end_time VARCHAR(32),
  data_as_of VARCHAR(32) NOT NULL DEFAULT '',
  batch_id VARCHAR(64) NOT NULL DEFAULT '',
  content_fingerprint VARCHAR(64) NOT NULL DEFAULT '',
  idempotency_key VARCHAR(128) NOT NULL DEFAULT '',
  status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  version INTEGER NOT NULL DEFAULT 1,
  created_by VARCHAR(128) NOT NULL DEFAULT '',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (owner_user_id, idempotency_key)
)`,
      `CREATE INDEX IF NOT EXISTS idx_insight_improvement_owner_bot ON insight_improvement_item (owner_user_id, bot_id, status, gmt_create)`,
      `CREATE TABLE IF NOT EXISTS insight_improvement_evidence (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  improvement_id INTEGER NOT NULL,
  session_id VARCHAR(128) NOT NULL DEFAULT '',
  task_index INTEGER NOT NULL DEFAULT 0,
  ordinal INTEGER NOT NULL DEFAULT 0,
  task_description_snapshot VARCHAR(1000) NOT NULL DEFAULT '',
  failure_class_snapshot VARCHAR(64) NOT NULL DEFAULT '',
  reasoning_summary VARCHAR(1000),
  payload_ref VARCHAR(512) NOT NULL DEFAULT '',
  payload_etag VARCHAR(128) NOT NULL DEFAULT '',
  payload_version_id VARCHAR(128),
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (improvement_id, session_id, task_index)
)`,
      `CREATE INDEX IF NOT EXISTS idx_insight_evidence_session_task ON insight_improvement_evidence (session_id, task_index)`,
      `CREATE INDEX IF NOT EXISTS idx_insight_evidence_improvement_order ON insight_improvement_evidence (improvement_id, ordinal)`,
      `CREATE TABLE IF NOT EXISTS insight_improvement_evolve_link (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  improvement_id INTEGER NOT NULL,
  evolve_task_id VARCHAR(64) NOT NULL DEFAULT '',
  request_id VARCHAR(128) NOT NULL DEFAULT '',
  created_by VARCHAR(128) NOT NULL DEFAULT '',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (improvement_id, evolve_task_id),
  UNIQUE (improvement_id, request_id)
)`,
      `CREATE INDEX IF NOT EXISTS idx_insight_evolve_task_id ON insight_improvement_evolve_link (evolve_task_id)`,
    ],
  },

  {
    version: 76,
    description: "Add Insight Center DB-backed failure task index",
    sql: [
      `CREATE TABLE IF NOT EXISTS insight_failure_task (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  source_dt VARCHAR(8) NOT NULL DEFAULT '',
  owner_user_id VARCHAR(128) NOT NULL DEFAULT '',
  bot_id VARCHAR(128) NOT NULL DEFAULT '',
  bot_name VARCHAR(256) NOT NULL DEFAULT '',
  session_id VARCHAR(128) NOT NULL DEFAULT '',
  task_index INTEGER NOT NULL DEFAULT 0,
  task_description VARCHAR(1000) NOT NULL DEFAULT '',
  is_complete INTEGER NOT NULL DEFAULT 2,
  failure_class VARCHAR(64) NOT NULL DEFAULT 'UNKNOWN',
  judge_reason_summary VARCHAR(1000),
  session_start_time VARCHAR(32),
  session_end_time VARCHAR(32),
  session_duration_seconds INTEGER,
  is_cron INTEGER NOT NULL DEFAULT 0,
  payload_ref VARCHAR(512) NOT NULL DEFAULT '',
  payload_etag VARCHAR(128) NOT NULL DEFAULT '',
  payload_version_id VARCHAR(128),
  batch_id VARCHAR(64) NOT NULL DEFAULT '',
  data_as_of VARCHAR(32) NOT NULL DEFAULT '',
  judged_at VARCHAR(32),
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (owner_user_id, session_id, task_index)
)`,
      `CREATE INDEX IF NOT EXISTS idx_insight_failure_owner_dt ON insight_failure_task (owner_user_id, source_dt, is_complete)`,
      `CREATE INDEX IF NOT EXISTS idx_insight_failure_owner_bot_dt ON insight_failure_task (owner_user_id, bot_id, source_dt, is_complete)`,
      `CREATE INDEX IF NOT EXISTS idx_insight_failure_session_task ON insight_failure_task (owner_user_id, session_id, task_index)`,
    ],
  },

  {
    version: 77,
    description: "Add Insight Center DB-backed daily metric snapshots",
    sql: [
      `CREATE TABLE IF NOT EXISTS insight_metric_daily (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  source_dt VARCHAR(8) NOT NULL DEFAULT '',
  owner_user_id VARCHAR(128) NOT NULL DEFAULT '',
  bot_id VARCHAR(128) NOT NULL DEFAULT '',
  bot_name VARCHAR(256) NOT NULL DEFAULT '',
  is_cron INTEGER NOT NULL DEFAULT 0,
  total_task_count DECIMAL(20,6) NOT NULL DEFAULT 0,
  valid_task_count DECIMAL(20,6) NOT NULL DEFAULT 0,
  complete_task_count DECIMAL(20,6) NOT NULL DEFAULT 0,
  capability_task_count DECIMAL(20,6) NOT NULL DEFAULT 0,
  capability_complete_task_count DECIMAL(20,6) NOT NULL DEFAULT 0,
  auto_complete_task_count DECIMAL(20,6) NOT NULL DEFAULT 0,
  failure_distribution_json TEXT DEFAULT NULL,
  batch_id VARCHAR(64) NOT NULL DEFAULT '',
  data_as_of VARCHAR(32) NOT NULL DEFAULT '',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (owner_user_id, bot_id, source_dt, is_cron)
)`,
      `CREATE INDEX IF NOT EXISTS idx_insight_metric_owner_dt ON insight_metric_daily (owner_user_id, source_dt, is_cron)`,
      `CREATE INDEX IF NOT EXISTS idx_insight_metric_owner_bot_dt ON insight_metric_daily (owner_user_id, bot_id, source_dt, is_cron)`,
    ],
  },

  {
    version: 78,
    description: "Align local Insight improvement tables with the streamlined evidence contract",
    sqliteOnly: true,
    sql: [
      `ALTER TABLE insight_improvement_evidence DROP COLUMN message_start`,
      `ALTER TABLE insight_improvement_evidence DROP COLUMN message_end`,
      `ALTER TABLE insight_improvement_evidence DROP COLUMN source_dt`,
      `ALTER TABLE insight_improvement_evidence DROP COLUMN batch_id`,
      `ALTER TABLE insight_improvement_evidence ADD COLUMN gmt_modified INTEGER NOT NULL DEFAULT 0`,
      `UPDATE insight_improvement_evidence SET gmt_modified = gmt_create WHERE gmt_modified = 0`,
      `ALTER TABLE insight_improvement_evolve_link ADD COLUMN gmt_modified INTEGER NOT NULL DEFAULT 0`,
      `UPDATE insight_improvement_evolve_link SET gmt_modified = gmt_create WHERE gmt_modified = 0`,
    ],
  },

  {
    version: 79,
    description: "Add Insight improvement Apply result audit fields",
    sql: [
      `ALTER TABLE insight_improvement_item ADD COLUMN applied_evolve_task_id VARCHAR(64)`,
      `ALTER TABLE insight_improvement_item ADD COLUMN apply_request_id VARCHAR(128)`,
      `ALTER TABLE insight_improvement_item ADD COLUMN applied_by VARCHAR(128)`,
      `ALTER TABLE insight_improvement_item ADD COLUMN applied_at TIMESTAMP`,
    ],
  },

  {
    version: 80,
    description: "Separate Insight improvement assignee from Bot owner",
    sql: [
      `ALTER TABLE insight_improvement_item ADD COLUMN bot_owner_user_id VARCHAR(128) NOT NULL DEFAULT ''`,
      `UPDATE insight_improvement_item SET bot_owner_user_id = owner_user_id WHERE bot_owner_user_id = ''`,
    ],
  },

  {
    version: 81,
    description: "Add optimistic locking version column to node_executions",
    sql: [
      `ALTER TABLE node_executions ADD COLUMN version BIGINT NOT NULL DEFAULT 1`,
    ],
  },
  {
    version: 82,
    description: "Create workflow_health_snapshots table for health score trend tracking",
    sql: [
      `CREATE TABLE IF NOT EXISTS workflow_health_snapshots (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  snapshot_date VARCHAR(10) NOT NULL,
  overall_score INTEGER NOT NULL,
  success_rate DECIMAL(5,2) NOT NULL,
  node_failure_rate DECIMAL(5,4) NOT NULL,
  p95_duration_ms BIGINT,
  retry_rate DECIMAL(5,4),
  total_tokens BIGINT,
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE (workflow_id, snapshot_date)
)`,
      `CREATE INDEX IF NOT EXISTS idx_wh_snapshots_wf_date ON workflow_health_snapshots (workflow_id, snapshot_date)`,
    ],
  },

  // Version 83 was used by a reverted self-repair schema change. Do not reuse it;
  // the next real migration must start at version 84 for environment compatibility.
  {
    version: 84,
    description: "Add Evolve frozen task source ledger",
    sql: [
      `CREATE TABLE IF NOT EXISTS ce_task_sources (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  task_id VARCHAR(64) NOT NULL,
  source_type VARCHAR(32) NOT NULL,
  source_id VARCHAR(128) NOT NULL DEFAULT '',
  source_schema_version VARCHAR(64) NOT NULL,
  adapter_version VARCHAR(64),
  source_ref_json MEDIUMTEXT NOT NULL,
  source_digest VARCHAR(80),
  status VARCHAR(32) NOT NULL DEFAULT 'pending',
  error_code VARCHAR(128),
  error_message TEXT,
  resolved_at INTEGER,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (task_id)
)`,
      `CREATE INDEX IF NOT EXISTS idx_ce_task_sources_origin ON ce_task_sources (source_type, source_id)`,
      `CREATE INDEX IF NOT EXISTS idx_ce_task_sources_status ON ce_task_sources (status, gmt_create)`,
    ],
  },
  {
    version: 85,
    description: "Add MySQL indexes for Evolve task source ledger",
    mysqlOnly: true,
    sql: [
      // v84 already created these indexes via CREATE INDEX IF NOT EXISTS (rendered as
      // CREATE INDEX for MySQL). No further DDL is needed for MySQL here.
    ],
  },
  {
    version: 86,
    description: "Add scoped Insight governance trust and persistent auto-repair grants",
    sql: [
      `CREATE TABLE IF NOT EXISTS insight_governance_rule_trust (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  scope_fingerprint VARCHAR(64) NOT NULL COMMENT '规则快照指纹',
  environment VARCHAR(32) NOT NULL COMMENT '运行环境',
  source_rule_id VARCHAR(64) NOT NULL COMMENT '治理规则ID',
  rule_version INTEGER NOT NULL COMMENT '治理规则版本',
  action_type VARCHAR(32) NOT NULL COMMENT '治理动作类型',
  allowed_targets_json MEDIUMTEXT NOT NULL COMMENT '允许修改目标快照',
  risk VARCHAR(16) NOT NULL COMMENT '风险等级快照',
  status VARCHAR(16) NOT NULL DEFAULT 'ACTIVE' COMMENT 'ACTIVE或REVOKED',
  trusted_by VARCHAR(128) NOT NULL COMMENT '信任操作人',
  trusted_at TIMESTAMP NOT NULL COMMENT '信任时间',
  version INTEGER NOT NULL DEFAULT 1 COMMENT '乐观锁版本',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()) COMMENT '创建时间',
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()) COMMENT '修改时间',
  UNIQUE (scope_fingerprint)
)`,
      `CREATE TABLE IF NOT EXISTS insight_auto_repair_grant (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  scope_fingerprint VARCHAR(64) NOT NULL COMMENT '用户授权范围指纹',
  owner_user_id VARCHAR(128) NOT NULL COMMENT '授权用户',
  bot_id VARCHAR(128) NOT NULL COMMENT '授权Bot',
  environment VARCHAR(32) NOT NULL COMMENT '运行环境',
  source_rule_id VARCHAR(64) NOT NULL COMMENT '治理规则ID',
  rule_version INTEGER NOT NULL COMMENT '治理规则版本',
  action_type VARCHAR(32) NOT NULL COMMENT '治理动作类型',
  allowed_targets_json MEDIUMTEXT NOT NULL COMMENT '允许修改目标快照',
  risk VARCHAR(16) NOT NULL COMMENT '风险等级快照',
  status VARCHAR(16) NOT NULL DEFAULT 'ACTIVE' COMMENT 'ACTIVE或REVOKED',
  source_improvement_id BIGINT NOT NULL COMMENT '授权来源改进项ID',
  granted_by VARCHAR(128) NOT NULL COMMENT '授权操作人',
  granted_at TIMESTAMP NOT NULL COMMENT '授权时间',
  revoked_by VARCHAR(128) COMMENT '撤销操作人',
  revoked_at TIMESTAMP COMMENT '撤销时间',
  version INTEGER NOT NULL DEFAULT 1 COMMENT '乐观锁版本',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()) COMMENT '创建时间',
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()) COMMENT '修改时间',
  UNIQUE (scope_fingerprint)
)`,
    ],
  },
  {
    version: 87,
    description: "Add Repair tool-call audit and execution ledger",
    sql: [
      `CREATE TABLE IF NOT EXISTS ce_repair_tool_calls (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  call_id VARCHAR(64) NOT NULL COMMENT '工具调用ID',
  task_id VARCHAR(64) NOT NULL COMMENT 'CE Task ID',
  step_id VARCHAR(64) NOT NULL COMMENT 'CE Step ID',
  execution_id VARCHAR(64) NOT NULL COMMENT 'AIS execution ID',
  authorization_scope_digest VARCHAR(64) NOT NULL COMMENT '用户Bot环境授权范围摘要',
  client_request_id VARCHAR(128) NOT NULL COMMENT 'Agent稳定幂等键',
  tool_name VARCHAR(64) NOT NULL COMMENT '类型化工具名',
  operation VARCHAR(256) NOT NULL COMMENT '类型化操作名',
  action_id VARCHAR(128) COMMENT '获批Plan动作ID',
  deadline_at BIGINT COMMENT '调用截止时间Unix秒',
  request_json MEDIUMTEXT NOT NULL COMMENT '脱敏且受限的执行请求或OSS引用',
  is_write INTEGER NOT NULL DEFAULT 0 COMMENT '是否写操作',
  status VARCHAR(32) NOT NULL DEFAULT 'pending' COMMENT '调用状态',
  lease_owner VARCHAR(128) COMMENT '当前执行租约持有者',
  lease_expires_at BIGINT COMMENT '执行租约截止时间Unix秒',
  result_json MEDIUMTEXT COMMENT '脱敏且受限的终态信封或OSS引用',
  result_digest VARCHAR(64) COMMENT '完整终态信封摘要',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()) COMMENT '创建时间',
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()) COMMENT '修改时间',
  UNIQUE (call_id),
  UNIQUE (step_id, client_request_id)
)`,
    ],
  },
  {
    version: 90,
    description: "Add first-class Evolve Pack registry",
    sql: [
      `CREATE TABLE IF NOT EXISTS ce_packs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  pack_id VARCHAR(64) NOT NULL,
  user_id VARCHAR(128) NOT NULL,
  bot_id VARCHAR(128) NOT NULL,
  source_task_id VARCHAR(64) NOT NULL,
  source_step_id VARCHAR(64) NOT NULL,
  source_kind VARCHAR(16) NOT NULL,
  source_round INTEGER NOT NULL DEFAULT 0,
  artifact_ref VARCHAR(1000) NOT NULL,
  artifact_size BIGINT NOT NULL,
  artifact_sha256 VARCHAR(64) NOT NULL,
  artifact_content_type VARCHAR(128) NOT NULL,
  status VARCHAR(16) NOT NULL DEFAULT 'available',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (pack_id),
  UNIQUE (source_task_id, source_step_id, source_kind, source_round)
)`,
      `CREATE INDEX IF NOT EXISTS idx_ce_packs_user_bot_time ON ce_packs (user_id, bot_id, gmt_create)`,
    ],
  },
  {
    version: 91,
    description: "Add MySQL indexes for Evolve Pack registry",
    mysqlOnly: true,
    sql: [
      // v90 already created this index via CREATE INDEX IF NOT EXISTS (rendered as
      // CREATE INDEX for MySQL). No further DDL is needed for MySQL here.
    ],
  },
  {
    version: 92,
    description: "Backfill historical Evolve Packs — SQLite",
    sqliteOnly: true,
    sql: [
      `INSERT OR IGNORE INTO ce_packs
       (pack_id, user_id, bot_id, source_task_id, source_step_id, source_kind, source_round,
        artifact_ref, artifact_size, artifact_sha256, artifact_content_type, status, gmt_create, gmt_modified)
       SELECT 'PACK-MIG-' || UPPER(HEX(RANDOMBLOB(16))), t.user_id, t.bot_id, t.task_id, s.step_id,
        'baseline', 0,
        json_extract(s.output_json, '$.baselineArtifact.artifact.ref'),
        json_extract(s.output_json, '$.baselineArtifact.artifact.size'),
        json_extract(s.output_json, '$.baselineArtifact.artifact.sha256'),
        json_extract(s.output_json, '$.baselineArtifact.artifact.contentType'),
        'available', COALESCE(s.completed_at, s.gmt_create), COALESCE(s.completed_at, s.gmt_create)
       FROM ce_steps s JOIN ce_tasks t ON t.task_id = s.task_id
       WHERE s.status = 'succeeded'
        AND s.step_type = 'optimize'
        AND s.round_no = 1
        AND json_extract(s.output_json, '$.baselineArtifact.status') = 'available'
        AND json_extract(s.output_json, '$.baselineArtifact.artifact.ref') IS NOT NULL`,
      `INSERT OR IGNORE INTO ce_packs
       (pack_id, user_id, bot_id, source_task_id, source_step_id, source_kind, source_round,
        artifact_ref, artifact_size, artifact_sha256, artifact_content_type, status, gmt_create, gmt_modified)
       SELECT 'PACK-MIG-' || UPPER(HEX(RANDOMBLOB(16))), t.user_id, t.bot_id, t.task_id, s.step_id,
        CASE WHEN t.task_type = 'pack' THEN 'snapshot' ELSE 'round' END,
        CASE WHEN t.task_type = 'pack' THEN 0 ELSE COALESCE(s.round_no, 0) END,
        json_extract(s.output_json, '$.pack.artifact.ref'),
        json_extract(s.output_json, '$.pack.artifact.size'),
        json_extract(s.output_json, '$.pack.artifact.sha256'),
        json_extract(s.output_json, '$.pack.artifact.contentType'),
        'available', COALESCE(s.completed_at, s.gmt_create), COALESCE(s.completed_at, s.gmt_create)
       FROM ce_steps s JOIN ce_tasks t ON t.task_id = s.task_id
       WHERE s.status = 'succeeded'
        AND s.step_type IN ('optimize', 'pack')
        AND json_extract(s.output_json, '$.pack.status') = 'available'
        AND json_extract(s.output_json, '$.pack.artifact.ref') IS NOT NULL`,
    ],
  },
  {
    version: 93,
    description: "Backfill historical Evolve Packs — MySQL",
    mysqlOnly: true,
    sql: [
      `INSERT IGNORE INTO ce_packs
       (pack_id, user_id, bot_id, source_task_id, source_step_id, source_kind, source_round,
        artifact_ref, artifact_size, artifact_sha256, artifact_content_type, status, gmt_create, gmt_modified)
       SELECT CONCAT('PACK-MIG-', UPPER(REPLACE(UUID(), '-', ''))), t.user_id, t.bot_id, t.task_id, s.step_id,
        'baseline', 0,
        JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.baselineArtifact.artifact.ref')),
        CAST(JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.baselineArtifact.artifact.size')) AS UNSIGNED),
        JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.baselineArtifact.artifact.sha256')),
        JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.baselineArtifact.artifact.contentType')),
        'available', COALESCE(s.completed_at, s.gmt_create), COALESCE(s.completed_at, s.gmt_create)
       FROM ce_steps s JOIN ce_tasks t ON t.task_id = s.task_id
       WHERE s.status = 'succeeded'
        AND s.step_type = 'optimize'
        AND s.round_no = 1
        AND JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.baselineArtifact.status')) = 'available'
        AND JSON_EXTRACT(s.output_json, '$.baselineArtifact.artifact.ref') IS NOT NULL`,
      `INSERT IGNORE INTO ce_packs
       (pack_id, user_id, bot_id, source_task_id, source_step_id, source_kind, source_round,
        artifact_ref, artifact_size, artifact_sha256, artifact_content_type, status, gmt_create, gmt_modified)
       SELECT CONCAT('PACK-MIG-', UPPER(REPLACE(UUID(), '-', ''))), t.user_id, t.bot_id, t.task_id, s.step_id,
        CASE WHEN t.task_type = 'pack' THEN 'snapshot' ELSE 'round' END,
        CASE WHEN t.task_type = 'pack' THEN 0 ELSE COALESCE(s.round_no, 0) END,
        JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.pack.artifact.ref')),
        CAST(JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.pack.artifact.size')) AS UNSIGNED),
        JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.pack.artifact.sha256')),
        JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.pack.artifact.contentType')),
        'available', COALESCE(s.completed_at, s.gmt_create), COALESCE(s.completed_at, s.gmt_create)
       FROM ce_steps s JOIN ce_tasks t ON t.task_id = s.task_id
       WHERE s.status = 'succeeded'
        AND s.step_type IN ('optimize', 'pack')
        AND JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.pack.status')) = 'available'
        AND JSON_EXTRACT(s.output_json, '$.pack.artifact.ref') IS NOT NULL`,
    ],
  },
  {
    version: 94,
    description: "ClawWeb: add state_json to flow_runs (engine state mirror for cross-instance reads)",
    sql: [
      // MEDIUMTEXT (16MB on MySQL; sqlite renders to TEXT) — TaskFlow state_json
      // routinely exceeds TEXT's 64KB MySQL cap once nodeStates carry results.
      `ALTER TABLE flow_runs ADD COLUMN state_json MEDIUMTEXT`,
    ],
  },
  {
    version: 95,
    description: "ClawWeb: flow_retry_requests mailbox for cross-engine retry handoff",
    sql: [
      `CREATE TABLE IF NOT EXISTS flow_retry_requests (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  request_id VARCHAR(255) NOT NULL,
  flow_id VARCHAR(255) NOT NULL,
  node_id VARCHAR(255),
  reason TEXT,
  requester VARCHAR(255),
  status VARCHAR(255) NOT NULL DEFAULT 'pending',
  claim_engine VARCHAR(255),
  claimed_at BIGINT,
  attempts INTEGER NOT NULL DEFAULT 0,
  result_message TEXT,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_flow_retry_requests_request_id ON flow_retry_requests (request_id)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_retry_requests_status ON flow_retry_requests (status)`,
      `CREATE INDEX IF NOT EXISTS idx_flow_retry_requests_flow_id ON flow_retry_requests (flow_id)`,
    ],
  },
  {
    version: 96,
    description: "Add clawweb_admin_users table for dynamic admin permission management",
    sql: [
      `CREATE TABLE IF NOT EXISTS clawweb_admin_users (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
  user_id VARCHAR(255) NOT NULL COMMENT '管理员用户标识(工号或域账号)',
  role VARCHAR(32) NOT NULL COMMENT '管理员角色: admin|log_admin|bench_admin|claw_evolve_admin',
  source VARCHAR(32) NOT NULL DEFAULT 'manual' COMMENT '来源: yaml_seed|manual',
  enabled TINYINT NOT NULL DEFAULT 1 COMMENT '是否启用: 1启用, 0禁用',
  created_by VARCHAR(255) DEFAULT NULL COMMENT '创建者用户标识',
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  UNIQUE INDEX uk_clawweb_admin_users_user_role (user_id, role),
  INDEX idx_clawweb_admin_users_role (role),
  INDEX idx_clawweb_admin_users_enabled (enabled),
  INDEX idx_clawweb_admin_users_user_id (user_id)
) COMMENT='ClawWeb动态管理员权限表'`,
    ],
  },
  {
    version: 97,
    description: "Backfill missed Optimize Round Packs — SQLite",
    sqliteOnly: true,
    sql: [
      `INSERT OR IGNORE INTO ce_packs
       (pack_id, user_id, bot_id, source_task_id, source_step_id, source_kind, source_round,
        artifact_ref, artifact_size, artifact_sha256, artifact_content_type, status, gmt_create, gmt_modified)
       SELECT 'PACK-MIG-' || UPPER(HEX(RANDOMBLOB(16))), t.user_id, t.bot_id, t.task_id, s.step_id,
        'round', COALESCE(s.round_no, 0),
        json_extract(s.output_json, '$.pack.artifact.ref'),
        json_extract(s.output_json, '$.pack.artifact.size'),
        json_extract(s.output_json, '$.pack.artifact.sha256'),
        json_extract(s.output_json, '$.pack.artifact.contentType'),
        'available', COALESCE(s.completed_at, s.gmt_create), COALESCE(s.completed_at, s.gmt_create)
       FROM ce_steps s JOIN ce_tasks t ON t.task_id = s.task_id
       WHERE s.status = 'succeeded'
        AND s.step_type = 'optimize'
        AND json_extract(s.output_json, '$.pack.status') = 'available'
        AND json_extract(s.output_json, '$.pack.artifact.ref') IS NOT NULL`,
    ],
  },
  {
    version: 98,
    description: "Backfill missed Optimize Round Packs — MySQL",
    mysqlOnly: true,
    sql: [
      `INSERT IGNORE INTO ce_packs
       (pack_id, user_id, bot_id, source_task_id, source_step_id, source_kind, source_round,
        artifact_ref, artifact_size, artifact_sha256, artifact_content_type, status, gmt_create, gmt_modified)
       SELECT CONCAT('PACK-MIG-', UPPER(REPLACE(UUID(), '-', ''))), t.user_id, t.bot_id, t.task_id, s.step_id,
        'round', COALESCE(s.round_no, 0),
        JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.pack.artifact.ref')),
        CAST(JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.pack.artifact.size')) AS UNSIGNED),
        JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.pack.artifact.sha256')),
        JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.pack.artifact.contentType')),
        'available', COALESCE(s.completed_at, s.gmt_create), COALESCE(s.completed_at, s.gmt_create)
       FROM ce_steps s JOIN ce_tasks t ON t.task_id = s.task_id
       WHERE s.status = 'succeeded'
        AND s.step_type = 'optimize'
        AND JSON_UNQUOTE(JSON_EXTRACT(s.output_json, '$.pack.status')) = 'available'
        AND JSON_EXTRACT(s.output_json, '$.pack.artifact.ref') IS NOT NULL`,
    ],
  },
  {
    version: 99,
    description: "Remove non-Round-1 Initial Pack registrations — SQLite",
    sqliteOnly: true,
    sql: [
      `DELETE FROM ce_packs
       WHERE source_kind = 'baseline'
        AND EXISTS (
          SELECT 1 FROM ce_steps s
          WHERE s.task_id = ce_packs.source_task_id
           AND s.step_id = ce_packs.source_step_id
           AND (s.step_type <> 'optimize' OR COALESCE(s.round_no, 0) <> 1)
        )`,
    ],
  },
  {
    version: 100,
    description: "Remove non-Round-1 Initial Pack registrations — MySQL",
    mysqlOnly: true,
    sql: [
      `DELETE p FROM ce_packs p
       INNER JOIN ce_steps s
        ON s.task_id = p.source_task_id AND s.step_id = p.source_step_id
       WHERE p.source_kind = 'baseline'
        AND (s.step_type <> 'optimize' OR COALESCE(s.round_no, 0) <> 1)`,
    ],
  },
  {
    version: 99,
    description: "Add Governance rule evolution proposals",
    sql: [
      `CREATE TABLE IF NOT EXISTS insight_rule_evolution_proposal (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  scope_fingerprint VARCHAR(64) NOT NULL COMMENT '规则快照指纹',
  environment VARCHAR(32) NOT NULL COMMENT '运行环境',
  source_rule_id VARCHAR(64) NOT NULL COMMENT '治理规则ID',
  from_rule_version INTEGER NOT NULL COMMENT '当前规则版本',
  proposed_rule_version INTEGER NOT NULL COMMENT '建议发布的规则版本',
  proposal_type VARCHAR(32) NOT NULL COMMENT '规则演进类型',
  action_type VARCHAR(32) NOT NULL COMMENT '治理动作类型',
  allowed_targets_json MEDIUMTEXT NOT NULL COMMENT '允许修改目标快照',
  risk VARCHAR(16) NOT NULL COMMENT '风险等级快照',
  success_count INTEGER NOT NULL DEFAULT 0 COMMENT '成功验收次数',
  owner_count INTEGER NOT NULL DEFAULT 0 COMMENT '覆盖Owner数量',
  bot_count INTEGER NOT NULL DEFAULT 0 COMMENT '覆盖Bot数量',
  last_verified_at TIMESTAMP NULL COMMENT '最近一次成功验收时间',
  rationale MEDIUMTEXT NOT NULL COMMENT '规则演进依据',
  status VARCHAR(16) NOT NULL DEFAULT 'PENDING' COMMENT 'PENDING、APPROVED或REJECTED',
  reviewed_by VARCHAR(128) COMMENT '审核人',
  reviewed_at TIMESTAMP NULL COMMENT '审核时间',
  review_comment MEDIUMTEXT COMMENT '审核说明',
  version INTEGER NOT NULL DEFAULT 1 COMMENT '乐观锁版本',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (scope_fingerprint, proposal_type, from_rule_version)
)`,
      `CREATE INDEX IF NOT EXISTS idx_insight_rule_evolution_status ON insight_rule_evolution_proposal (status, gmt_modified)`,
    ],
  },
  {
    version: 100,
    description: "Add http_callback_logs.callback_source column to distinguish workflow-level vs platform-level callbacks",
    sql: [
      `ALTER TABLE http_callback_logs ADD COLUMN callback_source VARCHAR(32) NOT NULL DEFAULT 'workflow-level'`,
      `CREATE INDEX IF NOT EXISTS idx_http_callback_logs_source ON http_callback_logs (callback_source)`,
    ],
  },
  {
    version: 101,
    description: "Add independent ClawEvolve task log archive records",
    sql: [
      `CREATE TABLE IF NOT EXISTS ce_task_log_archives (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        archive_id VARCHAR(128) NOT NULL UNIQUE,
        task_id VARCHAR(128) NOT NULL,
        active_key VARCHAR(128) UNIQUE,
        status VARCHAR(32) NOT NULL,
        requested_by VARCHAR(128) NOT NULL,
        transport VARCHAR(32),
        bot_run_id VARCHAR(256),
        bot_session_id VARCHAR(256),
        platform_response_json MEDIUMTEXT,
        artifact_ref MEDIUMTEXT,
        artifact_size BIGINT,
        artifact_sha256 VARCHAR(128),
        artifact_content_type VARCHAR(128),
        metadata_json MEDIUMTEXT,
        error_code VARCHAR(128),
        error_message MEDIUMTEXT,
        started_at BIGINT,
        completed_at BIGINT,
        gmt_create BIGINT NOT NULL,
        gmt_modified BIGINT NOT NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_ce_task_log_archives_task_time ON ce_task_log_archives (task_id, gmt_create)`,
      `CREATE INDEX IF NOT EXISTS idx_ce_task_log_archives_task_status ON ce_task_log_archives (task_id, status)`,
    ],
  },
  {
    version: 102,
    description: "Add is_active column to workflow_deploy_history for default version routing",
    mysqlOnly: true,
    sql: [
      `ALTER TABLE workflow_deploy_history ADD COLUMN is_active TINYINT NOT NULL DEFAULT 0`,
      `CREATE INDEX IF NOT EXISTS idx_wdh_workflow_active ON workflow_deploy_history (workflow_id, is_active)`,
    ],
  },
  {
    version: 103,
    description: "SQLite: create workflow_deploy_history table with is_active column + add version column to workflow_specs (MySQL got these in v64+v102)",
    sqliteOnly: true,
    sql: [
      `CREATE TABLE IF NOT EXISTS workflow_deploy_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  pack_id VARCHAR(255) NOT NULL,
  workflow_id VARCHAR(255) NOT NULL,
  deploy_number INTEGER NOT NULL,
  version INTEGER NOT NULL,
  tag_name VARCHAR(255),
  action VARCHAR(32) NOT NULL,
  from_deploy_number INTEGER,
  spec_json TEXT NOT NULL,
  note VARCHAR(512),
  bot_id VARCHAR(255),
  owner_id VARCHAR(255),
  is_active INTEGER NOT NULL DEFAULT 0,
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch())
)`,
      `CREATE UNIQUE INDEX IF NOT EXISTS uk_wdh_pack_deploy_wf ON workflow_deploy_history (pack_id, deploy_number, workflow_id)`,
      `CREATE INDEX IF NOT EXISTS idx_wdh_workflow_version ON workflow_deploy_history (workflow_id, version)`,
      `CREATE INDEX IF NOT EXISTS idx_wdh_workflow_active ON workflow_deploy_history (workflow_id, is_active)`,
      // SQLite also needs workflow_specs.version column (MySQL got it in v64)
      `ALTER TABLE workflow_specs ADD COLUMN version INTEGER`,
      `CREATE INDEX IF NOT EXISTS idx_wfs_workflow_version ON workflow_specs (workflow_id, version)`,
    ],
  },
  {
    version: 104,
    description: "Add workflow_version and workflow_deploy_number to flow_runs for multi-version tracking",
    sql: [
      `ALTER TABLE flow_runs ADD COLUMN workflow_version INTEGER`,
      `ALTER TABLE flow_runs ADD COLUMN workflow_deploy_number INTEGER`,
      `CREATE INDEX IF NOT EXISTS idx_flow_runs_wf_version ON flow_runs (workflow_id, workflow_version)`,
    ],
  },
  {
    version: 105,
    description: "Repair missing ClawEvolve task log archive storage",
    sql: [
      `CREATE TABLE IF NOT EXISTS ce_task_log_archives (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        archive_id VARCHAR(128) NOT NULL UNIQUE,
        task_id VARCHAR(128) NOT NULL,
        active_key VARCHAR(128) UNIQUE,
        status VARCHAR(32) NOT NULL,
        requested_by VARCHAR(128) NOT NULL,
        transport VARCHAR(32),
        bot_run_id VARCHAR(256),
        bot_session_id VARCHAR(256),
        platform_response_json MEDIUMTEXT,
        artifact_ref MEDIUMTEXT,
        artifact_size BIGINT,
        artifact_sha256 VARCHAR(128),
        artifact_content_type VARCHAR(128),
        metadata_json MEDIUMTEXT,
        error_code VARCHAR(128),
        error_message MEDIUMTEXT,
        started_at BIGINT,
        completed_at BIGINT,
        gmt_create BIGINT NOT NULL,
        gmt_modified BIGINT NOT NULL
      )`,
      `CREATE INDEX IF NOT EXISTS idx_ce_task_log_archives_task_time ON ce_task_log_archives (task_id, gmt_create)`,
      `CREATE INDEX IF NOT EXISTS idx_ce_task_log_archives_task_status ON ce_task_log_archives (task_id, status)`,
    ],
  },
  {
    version: 106,
    description: "Add owner-confirmed auto execution flag for scoped Insight repair grants",
    sql: [
      `ALTER TABLE insight_auto_repair_grant ADD COLUMN auto_execute INTEGER NOT NULL DEFAULT 0`,
      `CREATE INDEX IF NOT EXISTS idx_insight_auto_grant_auto_execute ON insight_auto_repair_grant (owner_user_id, bot_id, source_rule_id, status, auto_execute)`,
    ],
  },
  {
    version: 107,
    description: "Store verified repair guidance with governance rule evolution proposals",
    sql: [
      `ALTER TABLE insight_rule_evolution_proposal ADD COLUMN learned_fix TEXT`,
    ],
  },
  {
    version: 108,
    description: "Self-Evolution: lessons + suggestion_outcomes + failure_signatures + diagnosis_cards + repair_history + weakness_list + node_executions 补列 + node_attempts",
    sql: [
      `CREATE TABLE IF NOT EXISTS lessons (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
  failure_signature VARCHAR(256) NOT NULL COMMENT '失败签名:错误类型×节点类型×工具名',
  error_class VARCHAR(64) NOT NULL DEFAULT '' COMMENT '一级错误类目: param-type-mismatch|timeout|tool-not-found|contract-violation|auth|dependency-down|prompt-ambiguity',
  executor_type VARCHAR(64) NOT NULL DEFAULT '' COMMENT '执行器类型: embedded-agent|mcp-call|cli-script|...',
  tool_or_node VARCHAR(128) NOT NULL DEFAULT '' COMMENT '工具名或节点名',
  repair_type VARCHAR(32) NOT NULL COMMENT '修法类型: kb_hint|prompt_patch|arg_template_fix|node_patch|alert',
  repair_content MEDIUMTEXT NOT NULL COMMENT '修法内容(补丁后prompt/参数模板/节点结构调整)',
  confidence_score DECIMAL(5,4) NOT NULL DEFAULT 0.5000 COMMENT '可信度评分 0.0-1.0',
  hit_count INTEGER NOT NULL DEFAULT 0 COMMENT '命中总次数',
  success_count INTEGER NOT NULL DEFAULT 0 COMMENT '命中且成功次数',
  fail_count INTEGER NOT NULL DEFAULT 0 COMMENT '命中且失败次数',
  status VARCHAR(16) NOT NULL DEFAULT 'draft' COMMENT '状态: draft|validated|live|expired|suspended',
  evidence_run_ids TEXT COMMENT '支撑该经验的flowId列表(JSON)',
  source VARCHAR(32) NOT NULL DEFAULT 'manual' COMMENT '来源: manual|auto_heal|runtime_retry|batch_patch|auto_release',
  related_workflow_ids TEXT COMMENT '关联工作流ID列表(JSON)',
  metrics_before TEXT COMMENT '应用修法前的指标快照(JSON)',
  metrics_after TEXT COMMENT '应用修法后的指标快照(JSON)',
  bench_domain_id INTEGER COMMENT '评测域ID',
  last_hit_at INTEGER COMMENT '最近一次命中时间(unix秒)',
  last_hit_success INTEGER COMMENT '最近一次命中是否成功 0=否 1=是',
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  UNIQUE INDEX uk_lessons_sig_type (failure_signature, repair_type),
  INDEX idx_lessons_sig (failure_signature),
  INDEX idx_lessons_status (status, confidence_score),
  INDEX idx_lessons_hit (last_hit_at)
) COMMENT='经验库: 系统记忆故障修复经验'`,

      `CREATE TABLE IF NOT EXISTS suggestion_outcomes (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
  lesson_id INTEGER NOT NULL COMMENT '关联的lessons.id',
  workflow_id VARCHAR(255) NOT NULL COMMENT '工作流ID',
  node_id VARCHAR(255) NOT NULL DEFAULT '' COMMENT '节点ID',
  failure_signature VARCHAR(256) NOT NULL COMMENT '失败签名',
  adopted INTEGER NOT NULL DEFAULT 0 COMMENT '是否被采纳 1=是 0=否',
  applied_version VARCHAR(64) COMMENT '应用版本号',
  metrics_before TEXT COMMENT '应用前指标(JSON)',
  metrics_after TEXT COMMENT '应用后指标(JSON)',
  verdict VARCHAR(16) NOT NULL COMMENT '效果判定: improved|regressed|neutral|improved_after_retry',
  source VARCHAR(32) NOT NULL COMMENT '来源: runtime_retry|batch_patch|auto_release',
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  UNIQUE INDEX uk_outcome_lesson_wf_node_lesson (lesson_id, workflow_id, node_id),
  INDEX idx_outcome_lesson (lesson_id),
  INDEX idx_outcome_sig (failure_signature),
  INDEX idx_outcome_verdict (verdict, gmt_create),
  CONSTRAINT fk_outcome_lesson FOREIGN KEY (lesson_id) REFERENCES lessons(id) ON DELETE CASCADE
) COMMENT='强化信号表: 跨快慢两环的成绩单'`,

      `CREATE TABLE IF NOT EXISTS failure_signatures (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
  flow_id VARCHAR(255) NOT NULL COMMENT '流程ID',
  node_id VARCHAR(255) NOT NULL COMMENT '节点ID',
  failure_signature VARCHAR(256) NOT NULL COMMENT '失败签名: 错误类型×节点类型×工具名',
  error_class VARCHAR(64) NOT NULL DEFAULT '' COMMENT '一级错误类目',
  executor_type VARCHAR(64) NOT NULL DEFAULT '' COMMENT '执行器类型',
  tool_or_node VARCHAR(128) NOT NULL DEFAULT '' COMMENT '工具名或节点名',
  raw_error_sample VARCHAR(500) COMMENT '原始错误摘要(用于人工确认)',
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  UNIQUE INDEX uk_fs_flow_node (flow_id, node_id),
  INDEX idx_fs_sig (failure_signature)
) COMMENT='失败签名索引: 快速提取错误类型×节点×工具'`,

      `CREATE TABLE IF NOT EXISTS diagnosis_cards (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
  flow_id VARCHAR(255) NOT NULL COMMENT '流程ID',
  workflow_id VARCHAR(255) NOT NULL COMMENT '工作流ID',
  node_id VARCHAR(255) NOT NULL COMMENT '节点ID',
  failure_signature VARCHAR(256) NOT NULL COMMENT '失败签名',
  error_text TEXT NOT NULL COMMENT '错误文本',
  input_snapshot MEDIUMTEXT COMMENT '输入快照(JSON)',
  output_snapshot MEDIUMTEXT COMMENT '输出快照(JSON)',
  step_traces_snapshot MEDIUMTEXT COMMENT '步骤轨迹快照(JSON)',
  analysis_reasoning MEDIUMTEXT COMMENT '分析推理过程',
  suggested_repair_type VARCHAR(32) COMMENT '建议修法类型',
  suggested_repair_content MEDIUMTEXT COMMENT '建议修法内容',
  matched_lesson_id INTEGER COMMENT '命中的经验ID',
  outcome VARCHAR(16) NOT NULL DEFAULT 'not_recovered' COMMENT '结果: not_recovered|recovered',
  attempt_count INTEGER NOT NULL DEFAULT 0 COMMENT '总尝试次数',
  diagnosis_level VARCHAR(8) NOT NULL DEFAULT '' COMMENT '触发层级: L1|L2|L3',
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  INDEX idx_diag_sig (failure_signature),
  INDEX idx_diag_outcome (outcome),
  INDEX idx_diag_wf (workflow_id, gmt_create),
  INDEX idx_diag_hit (matched_lesson_id)
) COMMENT='诊断卡: 单次运行分析产物落库'`,

      `CREATE TABLE IF NOT EXISTS repair_history (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
  flow_id VARCHAR(255) NOT NULL COMMENT '流程ID',
  node_id VARCHAR(255) NOT NULL COMMENT '节点ID',
  failure_signature VARCHAR(256) NOT NULL COMMENT '失败签名',
  lesson_id INTEGER COMMENT '应用的经验ID',
  diagnosis_card_id INTEGER COMMENT '关联诊断卡ID',
  suggestion_outcome_id INTEGER COMMENT '关联强化信号ID',
  repair_type VARCHAR(32) NOT NULL COMMENT '修法类型: kb_hint|prompt_patch|arg_template_fix|node_patch|alert',
  repair_content MEDIUMTEXT COMMENT '修法内容',
  applied_by VARCHAR(32) NOT NULL COMMENT '应用方: guardian|auto_heal|evolution|manual',
  retry_success INTEGER COMMENT '重试是否成功 0=否 1=是 NULL=未尝试',
  level VARCHAR(8) NOT NULL DEFAULT '' COMMENT '触发层级: L1|L2|L3',
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  INDEX idx_repair_sig (failure_signature, gmt_create),
  INDEX idx_repair_success (retry_success, gmt_create),
  INDEX idx_repair_applied (applied_by, gmt_create)
) COMMENT='修复记录: 修法×效果×来源账本'`,

      `CREATE TABLE IF NOT EXISTS weakness_list (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
  failure_signature VARCHAR(256) NOT NULL COMMENT '失败签名(全局唯一)',
  error_class VARCHAR(64) NOT NULL DEFAULT '' COMMENT '一级错误类目',
  workflow_ids MEDIUMTEXT COMMENT '出现该弱点的workflowId列表(JSON数组)',
  occurrence_count INTEGER NOT NULL DEFAULT 0 COMMENT '出现总次数',
  affected_workflows_count INTEGER NOT NULL DEFAULT 0 COMMENT '受影响工作流数',
  repairability VARCHAR(16) NOT NULL DEFAULT 'auto' COMMENT '可修性: auto|semi|manual',
  priority_score DECIMAL(5,2) NOT NULL DEFAULT 0.00 COMMENT '优先级=log(occurrence+1)×affected_workflows_count',
  evidence_diagnosis_ids MEDIUMTEXT COMMENT '证据诊断卡ID列表(JSON数组)',
  latest_occurrence INTEGER COMMENT '最近一次发生时间(unix秒)',
  first_occurrence INTEGER COMMENT '首次发生时间(unix秒)',
  matched_lesson_ids MEDIUMTEXT COMMENT '已关联的经验ID列表(JSON数组)',
  status VARCHAR(16) NOT NULL DEFAULT 'active' COMMENT '状态: active|resolved|closed',
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  UNIQUE INDEX uk_weak_sig (failure_signature),
  INDEX idx_weak_priority (priority_score, status),
  INDEX idx_weak_status (status)
) COMMENT='弱点清单: batch分析跨run的ranking'`,

      `ALTER TABLE node_executions ADD COLUMN failure_signature VARCHAR(256) COMMENT '失败签名(自进化v108补列)'`,
      `ALTER TABLE node_executions ADD COLUMN repair_strategy VARCHAR(32) COMMENT '修法类型: kb_hint|prompt_patch|arg_template_fix|node_patch|alert'`,
      `ALTER TABLE node_executions ADD COLUMN is_recoverable INTEGER NOT NULL DEFAULT 0 COMMENT '是否可恢复 0=否 1=是'`,
      `ALTER TABLE node_executions ADD COLUMN matched_lesson_id INTEGER COMMENT '命中的经验ID'`,
      `ALTER TABLE node_executions ADD COLUMN resolved_args_json MEDIUMTEXT COMMENT '实际下发的参数模板(JSON)'`,
      `CREATE INDEX IF NOT EXISTS idx_node_exec_fs ON node_executions (failure_signature)`,

      `CREATE TABLE IF NOT EXISTS node_attempts (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
  flow_id VARCHAR(255) NOT NULL COMMENT '流程ID',
  node_id VARCHAR(255) NOT NULL COMMENT '节点ID',
  attempt INTEGER NOT NULL COMMENT '第几次重试(0-based)',
  error_text MEDIUMTEXT COMMENT '该次尝试的错误文本',
  duration_ms BIGINT COMMENT '该次尝试耗时(毫秒)',
  kb_lesson_ids_json MEDIUMTEXT COMMENT 'L1召回命中的lessonId列表(JSON)',
  did_succeed INTEGER NOT NULL DEFAULT 0 COMMENT '该次尝试是否成功 0=否 1=是',
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  UNIQUE INDEX uk_att_flow_node_attempt (flow_id, node_id, attempt),
  INDEX idx_att_flow_node (flow_id, node_id),
  CONSTRAINT fk_att_flow FOREIGN KEY (flow_id) REFERENCES flow_runs(flow_id) ON DELETE CASCADE
) COMMENT='节点重试留痕: append-only补充node_executions只保留latest的问题'`,
    ],
  },
  {
    version: 109,
    description: "Self-Evolution: bench candidate version lineage for auto-deploy gating",
    sql: [
      `CREATE TABLE IF NOT EXISTS cm_bench_candidate_versions (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
  workflow_id VARCHAR(255) NOT NULL COMMENT '工作流ID',
  lesson_id INTEGER NOT NULL COMMENT '触发的经验ID',
  candidate_version VARCHAR(64) NOT NULL COMMENT '候选版本号',
  score_vs_baseline DECIMAL(8,4) COMMENT '对基线版本的得分提升率',
  passed_baseline INTEGER NOT NULL DEFAULT 0 COMMENT '是否通过基线评测 1=是 0=否',
  overfit_detected INTEGER NOT NULL DEFAULT 0 COMMENT '是否检测到过拟合 1=是 0=否',
  deployed INTEGER NOT NULL DEFAULT 0 COMMENT '是否已发布 1=是 0=否',
  deploy_number INTEGER COMMENT '发布的deploy_number',
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  INDEX idx_cv_wf (workflow_id, gmt_create),
  INDEX idx_cv_lesson (lesson_id)
) COMMENT='候选版本谱系: 自动patch+bench+发布的每次进化记录'`,
    ],
  },
  {
    version: 110,
    description: "Add workflow evolution knowledge tables",
    sqliteOnly: true,
    sql: [
      `CREATE TABLE IF NOT EXISTS workflow_healing_lessons (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  lesson_id VARCHAR(64) NOT NULL COMMENT '经验ID',
  workflow_id VARCHAR(64) COMMENT '关联工作流ID',
  node_id VARCHAR(64) COMMENT '关联节点ID',
  failure_signature VARCHAR(512) NOT NULL COMMENT '失败签名',
  failure_mode VARCHAR(64) NOT NULL COMMENT '失败模式',
  executor_type VARCHAR(64) COMMENT '执行器类型',
  fix_kind VARCHAR(64) NOT NULL COMMENT '修法类型',
  fix_spec MEDIUMTEXT NOT NULL COMMENT '修法内容',
  status VARCHAR(32) NOT NULL DEFAULT 'draft' COMMENT '状态: draft|verified|published|retired',
  confidence INTEGER NOT NULL DEFAULT 0 COMMENT '信心分0-100',
  hit_count INTEGER NOT NULL DEFAULT 0 COMMENT '命中次数',
  rescued_count INTEGER NOT NULL DEFAULT 0 COMMENT '救回次数',
  note TEXT COMMENT '备注',
  created_by VARCHAR(128) COMMENT '创建人',
  updated_by VARCHAR(128) COMMENT '更新人',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (lesson_id)
)`,
      `CREATE INDEX IF NOT EXISTS idx_workflow_healing_lessons_workflow ON workflow_healing_lessons (workflow_id, status, gmt_create)`,
      `CREATE INDEX IF NOT EXISTS idx_workflow_healing_lessons_signature ON workflow_healing_lessons (failure_signature, failure_mode)`,
      `CREATE TABLE IF NOT EXISTS workflow_healing_diagnoses (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  diagnosis_id VARCHAR(64) NOT NULL COMMENT '诊断记录ID',
  flow_id VARCHAR(64) NOT NULL COMMENT '运行流ID',
  workflow_id VARCHAR(64) NOT NULL COMMENT '工作流ID',
  run_id VARCHAR(64) COMMENT 'Run ID',
  node_id VARCHAR(64) COMMENT '节点ID',
  failure_signature VARCHAR(512) NOT NULL COMMENT '失败签名',
  failure_mode VARCHAR(64) NOT NULL COMMENT '失败模式',
  executor_type VARCHAR(64) COMMENT '执行器类型',
  weak_node_id VARCHAR(64) COMMENT '薄弱节点ID',
  suggested_fix_kind VARCHAR(64) COMMENT '建议修法类型',
  lesson_id_hit VARCHAR(64) COMMENT '命中经验ID',
  error_text TEXT COMMENT '错误文本',
  created_by VARCHAR(128) COMMENT '创建人',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (diagnosis_id)
)`,
      `CREATE INDEX IF NOT EXISTS idx_workflow_healing_diagnoses_workflow ON workflow_healing_diagnoses (workflow_id, gmt_create)`,
      `CREATE INDEX IF NOT EXISTS idx_workflow_healing_diagnoses_flow ON workflow_healing_diagnoses (flow_id, gmt_create)`,
      `CREATE TABLE IF NOT EXISTS workflow_healing_outcomes (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  outcome_id VARCHAR(64) NOT NULL COMMENT '结果ID',
  lesson_id VARCHAR(64) NOT NULL COMMENT '经验ID',
  workflow_id VARCHAR(64) COMMENT '工作流ID',
  node_id VARCHAR(64) COMMENT '节点ID',
  action VARCHAR(64) NOT NULL COMMENT '动作',
  applied INTEGER NOT NULL DEFAULT 0 COMMENT '是否应用',
  succeeded INTEGER NOT NULL DEFAULT 0 COMMENT '是否成功',
  verdict VARCHAR(64) NOT NULL DEFAULT 'neutral' COMMENT '结果评价',
  note TEXT COMMENT '备注',
  created_by VARCHAR(128) COMMENT '创建人',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (outcome_id)
)`,
      `CREATE INDEX IF NOT EXISTS idx_workflow_healing_outcomes_lesson ON workflow_healing_outcomes (lesson_id, gmt_create)`,
    ],
  },
  {
    version: 111,
    description: "Ensure workflow_health_snapshots table exists (idempotent repair)",
    sqliteOnly: true,
    sql: [
      `CREATE TABLE IF NOT EXISTS workflow_health_snapshots (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(255) NOT NULL,
  snapshot_date VARCHAR(10) NOT NULL,
  overall_score INTEGER NOT NULL,
  success_rate DECIMAL(5,2) NOT NULL,
  node_failure_rate DECIMAL(5,4) NOT NULL,
  p95_duration_ms BIGINT,
  retry_rate DECIMAL(5,4),
  total_tokens BIGINT,
  gmt_create TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  gmt_modified TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE (workflow_id, snapshot_date)
)`,
      `CREATE INDEX IF NOT EXISTS idx_wh_snapshots_wf_date ON workflow_health_snapshots (workflow_id, snapshot_date)`,
    ],
  },
  {
    version: 112,
    description: "Add workflow_healing_suggestion_actions table",
    sqliteOnly: true,
    sql: [
      `CREATE TABLE IF NOT EXISTS workflow_healing_suggestion_actions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  signature VARCHAR(512) NOT NULL,
  workflow_id VARCHAR(64) NOT NULL,
  node_id VARCHAR(64),
  action VARCHAR(32) NOT NULL,
  fix_kind VARCHAR(64),
  note TEXT,
  created_by VARCHAR(128),
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (workflow_id, signature)
)`,
      `CREATE INDEX IF NOT EXISTS idx_wh_suggestion_actions_wf ON workflow_healing_suggestion_actions (workflow_id, gmt_create)`,
    ],
  },
  {
    version: 113,
    description: "Add flow_runs.evolution_analyzed_at for offline evolution analysis",
    sqliteOnly: true,
    sql: [
      `ALTER TABLE flow_runs ADD COLUMN evolution_analyzed_at INTEGER DEFAULT NULL`,
      `CREATE INDEX IF NOT EXISTS idx_flow_runs_evolution_analyzed ON flow_runs (workflow_id, evolution_analyzed_at)`,
    ],
  },
  {
    version: 114,
    description: "Add flow_runs.evolution_analysis_status for per-run analysis state",
    sqliteOnly: true,
    sql: [
      `ALTER TABLE flow_runs ADD COLUMN evolution_analysis_status VARCHAR(16) DEFAULT NULL`,
      `CREATE INDEX IF NOT EXISTS idx_flow_runs_evolution_status ON flow_runs (workflow_id, evolution_analysis_status)`,
    ],
  },
  {
    version: 115,
    description: "Add source column to workflow_healing_lessons",
    sqliteOnly: true,
    sql: [
      `ALTER TABLE workflow_healing_lessons ADD COLUMN source VARCHAR(32) DEFAULT 'log_analysis'`,
    ],
  },
  {
    version: 116,
    description: "Add workflow_healing_suggestions table for bot consumable repair specs",
    sqliteOnly: true,
    sql: [
      `CREATE TABLE IF NOT EXISTS workflow_healing_suggestions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  workflow_id VARCHAR(64) NOT NULL COMMENT '关联工作流ID',
  node_id VARCHAR(64) COMMENT '节点ID',
  weak_node_id VARCHAR(64) COMMENT '薄弱节点ID',
  failure_signature VARCHAR(512) NOT NULL COMMENT '失败签名',
  failure_mode VARCHAR(64) COMMENT '失败模式',
  fix_kind VARCHAR(64) COMMENT '修法类型',
  fix_spec TEXT COMMENT '修法内容',
  source_diagnosis_ids TEXT COMMENT '来源诊断ID列表 JSON',
  impact_run_ids TEXT COMMENT '影响运行ID列表 JSON',
  status VARCHAR(32) NOT NULL DEFAULT 'pending' COMMENT '状态: pending|adopted|rejected|benched|ignored|resolved',
  action_log TEXT COMMENT '操作日志 JSON 数组',
  created_by VARCHAR(128) COMMENT '创建人',
  updated_by VARCHAR(128) COMMENT '更新人',
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (workflow_id, failure_signature)
)`,
      `CREATE INDEX IF NOT EXISTS idx_wh_suggestions_wf_status ON workflow_healing_suggestions (workflow_id, status, gmt_create)`,
      `CREATE INDEX IF NOT EXISTS idx_wh_suggestions_signature ON workflow_healing_suggestions (workflow_id, failure_signature)`,
    ],
  },
  {
    version: 117,
    description: "Support suggestion outcomes in workflow_healing_outcomes (sqlite recreate)",
    sqliteOnly: true,
    sql: [
      `CREATE TABLE workflow_healing_outcomes_v108 (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  outcome_id VARCHAR(64) NOT NULL,
  lesson_id VARCHAR(64),
  suggestion_id VARCHAR(64),
  workflow_id VARCHAR(64),
  node_id VARCHAR(64),
  action VARCHAR(64) NOT NULL,
  applied INTEGER NOT NULL DEFAULT 0,
  succeeded INTEGER NOT NULL DEFAULT 0,
  verdict VARCHAR(64) NOT NULL DEFAULT 'neutral',
  note TEXT,
  source_task_id VARCHAR(64),
  source_step_id VARCHAR(64),
  created_by VARCHAR(128),
  gmt_create INTEGER NOT NULL DEFAULT (unixepoch()),
  gmt_modified INTEGER NOT NULL DEFAULT (unixepoch()),
  UNIQUE (outcome_id)
)`,
      `INSERT INTO workflow_healing_outcomes_v108 (id, outcome_id, lesson_id, workflow_id, node_id, action, applied, succeeded, verdict, note, created_by, gmt_create, gmt_modified) SELECT id, outcome_id, lesson_id, workflow_id, node_id, action, applied, succeeded, verdict, note, created_by, gmt_create, gmt_modified FROM workflow_healing_outcomes`,
      `DROP TABLE workflow_healing_outcomes`,
      `ALTER TABLE workflow_healing_outcomes_v108 RENAME TO workflow_healing_outcomes`,
      `CREATE INDEX idx_workflow_healing_outcomes_lesson ON workflow_healing_outcomes (lesson_id, gmt_create)`,
      `CREATE INDEX idx_workflow_healing_outcomes_suggestion ON workflow_healing_outcomes (suggestion_id, gmt_create)`,
    ],
  },
  {
    version: 118,
    description: "Separate suggestion application from effectiveness verification",
    sqliteOnly: true,
    sql: [
      `ALTER TABLE workflow_healing_suggestions ADD COLUMN applied_at INTEGER DEFAULT NULL`,
      `ALTER TABLE workflow_healing_suggestions ADD COLUMN verification_status VARCHAR(32) NOT NULL DEFAULT 'not_started'`,
      `ALTER TABLE workflow_healing_suggestions ADD COLUMN verification_checked_at INTEGER DEFAULT NULL`,
      `ALTER TABLE workflow_healing_suggestions ADD COLUMN recurrence_count INTEGER NOT NULL DEFAULT 0`,
      `ALTER TABLE workflow_healing_suggestions ADD COLUMN last_recurrence_at INTEGER DEFAULT NULL`,
      `CREATE INDEX IF NOT EXISTS idx_wh_suggestions_verification ON workflow_healing_suggestions (verification_status, applied_at)`,
    ],
  },

];
