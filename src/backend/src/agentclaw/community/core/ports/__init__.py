"""Outbound ports: what core calls *out* to, named by the caller's need.

Import the modules by name; this package deliberately re-exports nothing, so
that importing one port never drags in the rest.
"""
