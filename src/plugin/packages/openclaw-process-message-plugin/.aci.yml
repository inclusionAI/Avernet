# CI config for the OpenClaw process message plugin.
version: "2.0"

stages:
  - run

jobs:
  ci-engines-default:
    stage: run
    component: node-test
    id: ci-engines-default
    inputs:
      resourceClass: M
      nodeVersion: engines=default
      script: npm run ci
      env:
        NODE_ENV: test

workflow:
  stages:
    run:
