"""Test infrastructure column — the ``test`` / ``singlebox`` profile modules.

Binds the in-process ``plugins.local`` doubles. **Excluded from the community
distribution** — only the ``test`` / ``singlebox`` profiles import these.
"""
