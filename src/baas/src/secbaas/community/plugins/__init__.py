"""Plugin implementations — concrete realizations of SPI contracts.

Each sub-package mirrors ``spi/`` and provides production (``real/``) and
test (``stub/``) implementations.  Selected and wired in ``bootstrap/``
composition roots via validated configuration.
"""
