# `agentclaw.community.core.group_chat`

Group-chat domain (placeholder — single-box; orchestration is mostly delegated to BCS today).

## Context Boundary

```yaml
purpose: "Group-chat domain (placeholder — single-box; orchestration is mostly delegated to BCS today)."
provides:
  - "(minimal; expansion pending)"
consumes:
  []
internal_dependencies:
  []
```

### Change impact

Currently a near-empty module. Expansion here is the entry point for backend-side group-chat features that are not fully delegated to BCS.
