/**
 * taskPanel UMD entry. Mirrors bcsPanel/src/index.ts.
 * UMD library name `taskPanel` (vite.config.ts); named export `TaskWorkflowView`.
 * The skill emits `component="taskPanel.TaskWorkflowView" entry="TaskWorkflowView"`.
 */
export { default as TaskWorkflowView } from './TaskWorkflowView';
export type { TaskWorkflowViewProps } from './types';
