/**
 * Community default knowledge adapters.
 * Returns empty array — no external knowledge sources.
 * Corp extensions provide YuQue, AgentMind, etc.
 */
export function createCommunityKnowledgeAdapters(_config: unknown): unknown[] {
  return [];
}
