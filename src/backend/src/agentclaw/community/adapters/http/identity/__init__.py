"""Identity API package.

The router lives in :mod:`agentclaw.community.api.identity.router`. The canonical
schemas and constants are owned by
:mod:`agentclaw.community.core.services.identity` so they can be imported from
``core`` without flipping the layer direction.
"""
