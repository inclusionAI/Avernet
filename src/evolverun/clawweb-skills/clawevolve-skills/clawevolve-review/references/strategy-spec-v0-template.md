# Evolution Strategy Spec v0 Template

这是 `clawevolve-review` 旧入口兼容文件。现在的唯一共享模板放在：

- `../../clawevolve-plan/references/evolution-strategy-spec-v0-template.md`

请以后只维护共享模板，不要继续修改这里的重复正文。

`clawevolve-review` 仍然会消费 `spec-v{N-1}.md` 并输出 `spec-vN.md`，只是正文结构现在与 `clawevolve-plan`、`clawevolve-tune` 共用同一套模板。
