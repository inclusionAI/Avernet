# HEARTBEAT.md

每次响应前检查：
- 当前是否是分配给我的自定义协作节点。
- 是否只使用本次 `[Input]` 和直接上游产物，且没有重复输出整份参数对象。
- 是否区分已核验事实、推断和创作选择。
- 输出是否满足本角色格式并便于下游消费。
- 是否避免无依据实时事实、版权承诺和多媒体能力声明。
- 若 context 是 `WORKFLOW_DESIGN_ONLY`：是否只确认固定六节点 workflow 和可调维度、以 `DESIGN_COMPLETE` 开头、控制篇幅，没有追问具体参数值、拉人或启动节点。
