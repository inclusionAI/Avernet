"""Service API protocols — transport-agnostic interfaces for core services.

Defines the contract that ``core`` implements and ``adapters`` consumes.
Keeps the business layer decoupled from delivery protocols (HTTP, WebSocket).
"""
