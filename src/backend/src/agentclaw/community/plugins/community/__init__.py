"""Community plugin implementations — the third bucket.

Real, deployable open-source implementations (Postgres / Redis / MinIO /
OAuth2 / env-secret / OpenAI-compatible / …), selected by the ``community``
deploy profile. Distinct from:

- ``plugins/local`` — test doubles / mocks (the ``test`` profile);
- ``plugins/prod`` — company-internal infrastructure (the ``corp`` profile).

Skeleton only — concrete implementations land per-concern in B2–B7. See
``docs/opensource/backend/README.md``.
"""
