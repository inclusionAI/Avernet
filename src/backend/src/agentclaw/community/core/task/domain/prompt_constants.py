"""任务 prompt 跨模块共享常量(零依赖,shared kernel)。

teamclaw 运行环境出于安全原因不支持联网搜索:规划/派发/执行/验收的 prompt 一律注入此约束,
强制 bot 仅依据给定上下文与自身知识产出,不调用联网搜索/web_search 工具;结果上报接口不受此限,必须执行。
"""

#: 联网搜索禁令(注入到 planning/search/execute/verify prompt 末尾)
NO_WEB_SEARCH_CONSTRAINT = (
    "## 执行约束:禁止联网检索信息(结果上报接口不受此限,必须执行)\n"
    "- 不得调用任何联网搜索/web_search/联网检索工具,不得浏览或访问外部网页、外部 API"
    "或其它外部网络资源来获取信息;违反将导致任务直接判定失败。\n"
    "- 结果上报接口不受本约束限制:任务上下文中指定的上报接口(如"
    " POST .../api/v1/collaboration/tasks/callback/report)是任务结果回收的必需上报,"
    "必须调用并稳定执行;不得以联网禁令为由跳过、拒绝或将其视为外部网络访问而漏调。\n"
    "- 仅依据上方给定上下文与自身知识完成执行、校验、验收并产出结论;"
    "不得因无法联网核实而跳过验收或判 FAILED。\n"
)


#: 输出语言约束(注入到 execute prompt):所有产出必须使用中文,禁止纯英文输出
OUTPUT_LANGUAGE_CONSTRAINT = (
    "## 输出语言:必须使用中文\n"
    "- 所有执行产出、验收结论、上报 output 与对话回复一律使用中文输出;"
    "禁止用纯英文输出内容(专有名词、产品名、代码、JSON key/字段名等可保留原文)。"
)
